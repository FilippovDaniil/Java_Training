package m01_complexity_big_o.practice;

/**
 * Задача 03 — Тема 01: Простой бенчмарк — замер времени для разных n
 *
 * ЗАДАНИЕ:
 *   Напишите метод sumPairs(int[] a), который считает сумму всех произведений
 *   a[i]*a[j] (вложенные циклы — O(n^2)). Затем в main замерьте время его
 *   работы через System.nanoTime() для n = 1000, 2000, 4000, 8000 и выведите
 *   таблицу: n, время (мс), отношение времени к предыдущему замеру.
 *
 * ПРИМЕР / ВЫВОД (числа зависят от железа — важна тенденция):
 *   n=1000   t=  3.1 ms   x-
 *   n=2000   t= 12.4 ms   x4.0
 *   n=4000   t= 49.7 ms   x4.0
 *   n=8000   t=198.0 ms   x4.0
 *   (при удвоении n время растёт ~в 4 раза — это и есть O(n^2))
 *
 * ТРЕБОВАНИЯ:
 *   - данные генерируйте сами (например, a[i] = i);
 *   - время в миллисекундах = наносекунды / 1_000_000.0;
 *   - выведите отношение текущего времени к предыдущему (для O(n^2) ожидается ~4).
 *
 * ПОДСКАЗКА:
 *   long start = System.nanoTime(); ... long end = System.nanoTime();
 *   double ms = (end - start) / 1_000_000.0;
 *   Прогоните «прогрев» (один лишний запуск) — JIT компилирует код не сразу.
 */

public class Task03 {

    static long sumPairs(int[] a) {
        long s = 0;
        // TODO: вложенные циклы по i и j, s += (long) a[i] * a[j]
        return s;
    }

    public static void main(String[] args) {
        int[] sizes = {1000, 2000, 4000, 8000};
        // TODO: для каждого размера: сгенерировать массив, замерить время sumPairs,
        //       вывести n, время в мс и отношение к предыдущему замеру
    }
}
