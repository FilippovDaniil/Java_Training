package m01_complexity_big_o.practice;

/**
 * Задача 04 — Тема 01: Сравнить линейный и квадратичный рост графически
 *
 * ЗАДАНИЕ:
 *   Постройте в консоли «график» из звёздочек, показывающий, как растёт число
 *   операций у линейного (O(n)) и квадратичного (O(n^2)) алгоритмов.
 *   Для n = 1..8 выведите две строки: linear(n) и quad(n), где длина строки
 *   звёздочек пропорциональна числу операций (масштабируйте, чтобы помещалось).
 *
 * ПРИМЕР / ВЫВОД:
 *   n=1 | O(n)   *
 *   n=1 | O(n^2) *
 *   ...
 *   n=8 | O(n)   ********
 *   n=8 | O(n^2) (намного длиннее — обрезать по ширине, напечатать число)
 *
 * ТРЕБОВАНИЯ:
 *   - linear(n) возвращает n; quad(n) возвращает n*n;
 *   - рисуйте бар из min(value, MAX_WIDTH) звёздочек, рядом печатайте само число;
 *   - наглядно видно, что квадратичный бар обгоняет линейный.
 *
 * ПОДСКАЗКА:
 *   String bar = "*".repeat(Math.min(value, 50));
 */

public class Task04 {

    static int linear(int n) { return n; }
    static int quad(int n) { return n * n; }

    static String bar(int value, int maxWidth) {
        // TODO: вернуть строку из min(value, maxWidth) звёздочек
        return "";
    }

    public static void main(String[] args) {
        // TODO: для n=1..8 вывести две строки (linear и quad) с барами и числами
    }
}
