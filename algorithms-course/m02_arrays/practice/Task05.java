package m02_arrays.practice;

/**
 * Задача 05 — Тема 02: Слияние двух отсортированных массивов
 *
 * ЗАДАНИЕ:
 *   Слейте два ОТСОРТИРОВАННЫХ массива a и b в один отсортированный.
 *   1) merge(a, b) — классическое слияние двумя указателями за O(n+m),
 *      результат в новом массиве.
 *   2) (обсуждение в комментарии) можно ли слить «in-place» без доп. памяти,
 *      если результат должен поместиться в a с запасом? Опишите идею в TODO.
 *
 * ПРИМЕР / ВЫВОД:
 *   a = [1, 3, 5], b = [2, 4, 6]
 *   merge = [1, 2, 3, 4, 5, 6]
 *
 * ТРЕБОВАНИЯ:
 *   - оба входа уже отсортированы по возрастанию;
 *   - результат отсортирован; длина = a.length + b.length;
 *   - слияние одним проходом (два указателя), без повторной сортировки.
 *
 * ПОДСКАЗКА:
 *   i, j — указатели по a и b; на каждом шаге берём меньший из a[i], b[j];
 *   в конце дописываем «хвост» того массива, что не закончился.
 */

import java.util.Arrays;

public class Task05 {

    static int[] merge(int[] a, int[] b) {
        int[] res = new int[a.length + b.length];
        // TODO: два указателя i, j; заполнить res меньшими элементами;
        //       дописать остаток
        return res;
    }

    public static void main(String[] args) {
        int[] a = {1, 3, 5};
        int[] b = {2, 4, 6};
        System.out.println("merge = " + Arrays.toString(merge(a, b)));
        // TODO (в комментарии): идея in-place слияния, если в a есть запас места
    }
}
