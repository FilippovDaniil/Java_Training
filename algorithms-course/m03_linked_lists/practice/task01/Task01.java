package m03_linked_lists.practice.task01;

/**
 * Задача 01 — Тема 03: Односвязный список (addFirst, addLast, removeFirst)
 *
 * ЗАДАНИЕ:
 *   Реализуйте класс SinglyLinkedList (файл SinglyLinkedList.java) с методами
 *   addFirst, addLast, removeFirst, size. Все три модификации — за O(1)
 *   (addLast — за счёт хранимого tail).
 *
 * ПРИМЕР / ВЫВОД:
 *   addLast 10, 20; addFirst 5  ->  [5, 10, 20], size=3
 *   removeFirst -> 5,  список [10, 20]
 *
 * ТРЕБОВАНИЯ:
 *   - addFirst/addLast/removeFirst — строго O(1);
 *   - removeFirst на пустом списке — бросить NoSuchElementException (или IllegalStateException);
 *   - поддерживайте head и tail согласованно (особенно когда список становится пустым).
 *
 * ПОДСКАЗКА:
 *   Когда удалили последний элемент, не забудьте tail = null.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: продемонстрируйте addLast/addFirst/removeFirst/size с выводом списка
    }
}
