package m03_linked_lists.practice.task07;

/**
 * Задача 07 (мини-проект Data-Cruncher) — Тема 03: LRU-кэш последних операций
 *
 * ЗАДАНИЕ:
 *   Реализуйте LruCache (файл LruCache.java) — кэш фиксированной ёмкости,
 *   который при переполнении вытесняет НАИМЕНЕЕ недавно использованный элемент.
 *   Операции get и put — за O(1) (двусвязный список + HashMap).
 *   В Data-Cruncher это кэш результатов последних операций анализа.
 *
 * ПРИМЕР / ВЫВОД:
 *   cap=2
 *   put(1,10); put(2,20); get(1)=10
 *   put(3,30)  // вытесняет ключ 2 (он был самым старым)
 *   get(2)=-1  // вытеснен
 *   get(3)=30
 *
 * ТРЕБОВАНИЯ:
 *   - get и put — амортизированно O(1);
 *   - доступ (get) и обновление (put) делают элемент «свежим»;
 *   - при превышении capacity вытесняется самый старый (LRU).
 *
 * ПОДСКАЗКА:
 *   Голова списка — свежие, хвост — старые. И get, и put перемещают узел в голову.
 */

public class Task07 {
    public static void main(String[] args) {
        LruCache cache = new LruCache(2);
        // TODO: воспроизведите сценарий из примера и проверьте вытеснение
    }
}
