package m04_stack_queue.practice;

/**
 * Задача 03 — Тема 04: Проверка сбалансированности скобок
 *
 * ЗАДАНИЕ:
 *   Проверьте, сбалансированы ли скобки в строке. Поддержите три вида:
 *   () [] {}. Скобки сбалансированы, если каждая открывающая закрыта
 *   соответствующей закрывающей в правильном порядке.
 *
 * ПРИМЕР / ВЫВОД:
 *   "(a[b]{c})"  -> true
 *   "([)]"       -> false  (неверный порядок)
 *   "((("        -> false  (не закрыты)
 *   "a + b"      -> true   (скобок нет — сбалансировано)
 *
 * ТРЕБОВАНИЯ:
 *   - используйте стек открывающих скобок;
 *   - на закрывающей: стек не пуст И вершина — парная открывающая;
 *   - в конце стек должен быть пуст;
 *   - не-скобочные символы игнорируются.
 *
 * ПОДСКАЗКА:
 *   Заведите соответствие закрывающая->открывающая: ')'→'(', ']'→'[', '}'→'{'.
 */

import java.util.ArrayDeque;
import java.util.Deque;

public class Task03 {

    static boolean isBalanced(String s) {
        Deque<Character> stack = new ArrayDeque<>();
        // TODO: пройти по символам; открывающие push; закрывающие — сверить с вершиной
        return stack.isEmpty();
    }

    public static void main(String[] args) {
        String[] tests = {"(a[b]{c})", "([)]", "(((", "a + b"};
        for (String t : tests)
            System.out.println("\"" + t + "\" -> " + isBalanced(t));
    }
}
