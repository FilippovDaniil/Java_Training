package m06_search_linear_binary.practice;

/**
 * Задача 05 — Тема 06: Поиск в 2D-матрице (строки и столбцы отсортированы)
 *
 * ЗАДАНИЕ:
 *   В матрице, где КАЖДАЯ строка отсортирована слева направо и КАЖДЫЙ столбец
 *   сверху вниз, найдите target за O(n + m) (не за O(n*m)!).
 *
 * ПРИМЕР / ВЫВОД:
 *   matrix:
 *     1  4  7 11
 *     2  5  8 12
 *     3  6  9 16
 *    10 13 14 17
 *   search(5)  -> найдено (строка 1, столбец 1)
 *   search(15) -> не найдено
 *
 * ТРЕБОВАНИЯ:
 *   - старт из правого верхнего угла (row=0, col=last);
 *   - если текущий > target -> влево (col--); если < target -> вниз (row++);
 *   - O(n + m); верните true/false (или координаты).
 *
 * ПОДСКАЗКА:
 *   Правый верхний элемент — самый большой в строке и самый маленький в столбце,
 *   поэтому каждое сравнение однозначно отсекает строку или столбец.
 */

public class Task05 {

    static boolean search(int[][] matrix, int target) {
        // TODO: начать с правого верхнего угла, двигаться влево/вниз
        return false;
    }

    public static void main(String[] args) {
        int[][] m = {
            {1, 4, 7, 11},
            {2, 5, 8, 12},
            {3, 6, 9, 16},
            {10, 13, 14, 17}
        };
        System.out.println("search(5) = " + search(m, 5));
        System.out.println("search(15) = " + search(m, 15));
    }
}
