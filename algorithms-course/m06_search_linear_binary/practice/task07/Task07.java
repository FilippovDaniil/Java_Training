package m06_search_linear_binary.practice.task07;

/**
 * Задача 07 (мини-проект Data-Cruncher) — Тема 06: поиск по ключу + бенчмарк
 *
 * ЗАДАНИЕ:
 *   Добавьте в Data-Cruncher SearchBenchmark (файл SearchBenchmark.java):
 *   поиск по ключу в загруженном массиве двумя способами и сравнение времени
 *   линейного (O(n)) и бинарного (O(log n)) поиска на большом наборе.
 *
 * ПРИМЕР / ВЫВОД (числа зависят от железа — важна тенденция):
 *   n=1_000_000, запросов=10_000
 *   Линейный: t=  820 ms
 *   Бинарный: t=    3 ms
 *   Бинарный быстрее в ~270 раз (для отсортированных данных).
 *
 * ТРЕБОВАНИЯ:
 *   - данные отсортированы (для бинарного поиска это обязательно);
 *   - выполнить много запросов, чтобы разница была заметна;
 *   - вывести время обоих методов и их отношение.
 *
 * ПОДСКАЗКА:
 *   Сгенерируйте отсортированный массив (например a[i]=i) и массив случайных
 *   запросов. Не забудьте «прогрев» перед замером (JIT).
 */

public class Task07 {
    public static void main(String[] args) {
        int n = 1_000_000;
        int[] data = new int[n];
        for (int i = 0; i < n; i++) data[i] = i;   // уже отсортирован
        // TODO: сгенерировать массив запросов, создать SearchBenchmark, вызвать benchmark(...)
    }
}
