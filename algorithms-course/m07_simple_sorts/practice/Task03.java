package m07_simple_sorts.practice;

/**
 * Задача 03 — Тема 07: Сортировка вставками (insertion sort)
 *
 * ЗАДАНИЕ:
 *   Реализуйте сортировку вставками: каждый следующий элемент «вставляйте»
 *   на место в уже отсортированную левую часть, сдвигая большие элементы вправо.
 *
 * ПРИМЕР / ВЫВОД:
 *   До:    [5, 3, 8, 1, 2]
 *   После: [1, 2, 3, 5, 8]
 *   На почти отсортированных данных — близко к O(n) (адаптивность).
 *
 * ТРЕБОВАНИЯ:
 *   - in-place; стабильная (строгое сравнение a[j] > key);
 *   - сдвиг элементов вправо до места вставки;
 *   - начинать с i=1 (элемент 0 считается отсортированным).
 *
 * ПОДСКАЗКА:
 *   key=a[i]; j=i-1; while(j>=0 && a[j]>key){ a[j+1]=a[j]; j--; } a[j+1]=key.
 */

import java.util.Arrays;

public class Task03 {

    static void insertionSort(int[] a) {
        // TODO: сортировка вставками
    }

    public static void main(String[] args) {
        int[] a = {5, 3, 8, 1, 2};
        insertionSort(a);
        System.out.println(Arrays.toString(a));
    }
}
