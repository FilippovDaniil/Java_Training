package m07_simple_sorts.practice;

/**
 * Задача 04 — Тема 07: Сравнить время на случайных и почти отсортированных данных
 *
 * ЗАДАНИЕ:
 *   Сравните сортировку вставками на ДВУХ типах входа одинакового размера:
 *     - случайный массив;
 *     - почти отсортированный (отсортированный, где переставлено лишь несколько пар).
 *   Замерьте время. Покажите, что на почти отсортированных вставки заметно быстрее
 *   (адаптивность).
 *
 * ПРИМЕР / ВЫВОД (числа зависят от железа — важна тенденция):
 *   n=50000
 *   случайный:           t= 410 ms
 *   почти отсортированный: t=   2 ms
 *   Вывод: insertion sort адаптивна — на почти готовых данных ~O(n).
 *
 * ТРЕБОВАНИЯ:
 *   - используйте insertion sort (можно скопировать из задачи 03);
 *   - «почти отсортированный»: возьмите 0,1,2,...,n-1 и переставьте ~1% случайных пар;
 *   - замер через System.nanoTime(); сортируйте КОПИИ, чтобы не испортить вход.
 *
 * ПОДСКАЗКА:
 *   Не забудьте прогрев (один лишний прогон) перед замерами — иначе JIT исказит числа.
 */

public class Task04 {

    static void insertionSort(int[] a) {
        // TODO: можно повторно использовать реализацию из задачи 03
    }

    public static void main(String[] args) {
        int n = 50_000;
        // TODO: построить случайный и почти отсортированный массивы,
        //       замерить insertionSort на копиях каждого, вывести времена и вывод
    }
}
