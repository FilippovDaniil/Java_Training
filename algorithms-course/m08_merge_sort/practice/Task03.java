package m08_merge_sort.practice;

/**
 * Задача 03 — Тема 08: Замер merge sort против пузырька на 100k элементов
 *
 * ЗАДАНИЕ:
 *   Сгенерируйте случайный массив на 100_000 элементов и отсортируйте его
 *   КОПИИ двумя способами: пузырьком (O(n²)) и merge sort (O(n log n)).
 *   Замерьте и сравните время. Разница должна быть в сотни/тысячи раз.
 *
 * ПРИМЕР / ВЫВОД (числа зависят от железа — важна тенденция):
 *   n=100000
 *   bubble:    t= 28000 ms
 *   mergeSort: t=    18 ms
 *   mergeSort быстрее в ~1500 раз.
 *
 * ТРЕБОВАНИЯ:
 *   - сортируйте КОПИИ одного и того же массива;
 *   - пузырёк можно взять из темы 07; merge sort — из задачи 01;
 *   - если пузырёк на 100k слишком долго — допустимо взять для него меньший n
 *     и экстраполировать, но обязательно поясните это в выводе.
 *
 * ПОДСКАЗКА:
 *   System.nanoTime() до и после; время в мс = разница / 1_000_000.0.
 */

public class Task03 {

    static void bubbleSort(int[] a) {
        // TODO: пузырёк (из темы 07)
    }

    static void mergeSort(int[] a, int lo, int hi) {
        // TODO: merge sort (из задачи 01)
    }

    public static void main(String[] args) {
        // TODO: сгенерировать массив, замерить обе сортировки на копиях, вывести сравнение
    }
}
