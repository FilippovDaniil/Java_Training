package m10_counting_radix_sort.practice;

/**
 * Задача 01 — Тема 10: Counting sort для чисел 0..1000
 *
 * ЗАДАНИЕ:
 *   Реализуйте сортировку подсчётом для массива неотрицательных целых в
 *   диапазоне [0, 1000]. Время O(n + k), стабильная.
 *
 * ПРИМЕР / ВЫВОД:
 *   До:    [5, 2, 9, 2, 0, 5, 1000, 1]
 *   После: [0, 1, 2, 2, 5, 5, 9, 1000]
 *
 * ТРЕБОВАНИЯ:
 *   - значения в [0, MAX], где MAX=1000; count размера MAX+1;
 *   - стабильность: проход исходного массива справа налево после префиксных сумм;
 *   - вернуть новый отсортированный массив (или отсортировать на месте копированием).
 *
 * ПОДСКАЗКА:
 *   1) count[x]++; 2) префиксные суммы; 3) out[--count[a[i]]] = a[i] справа налево.
 */

import java.util.Arrays;

public class Task01 {

    static final int MAX = 1000;

    static int[] countingSort(int[] a) {
        // TODO: частоты -> префиксные суммы -> раскладка справа налево
        return a.clone();
    }

    public static void main(String[] args) {
        int[] a = {5, 2, 9, 2, 0, 5, 1000, 1};
        System.out.println(Arrays.toString(countingSort(a)));
    }
}
