package m16_bitset_disjoint_set_union.practice;

/**
 * Задача 04 — Тема 16: Количество компонент связности через DSU
 *
 * ЗАДАНИЕ:
 *   Дан неориентированный граф (n вершин и список рёбер). Посчитайте число
 *   КОМПОНЕНТ СВЯЗНОСТИ с помощью DSU: union по каждому ребру; число компонент =
 *   число различных корней (или n минус число успешных объединений).
 *
 * ПРИМЕР / ВЫВОД:
 *   n=6, рёбра: (0-1), (1-2), (3-4)
 *   компоненты: {0,1,2}, {3,4}, {5}  -> 3 компоненты
 *
 * ТРЕБОВАНИЯ:
 *   - используйте DSU (можно со сжатием пути и рангом);
 *   - компонент = число уникальных find(i) по всем i;
 *   - корректно при изолированных вершинах (без рёбер).
 *
 * ПОДСКАЗКА:
 *   Либо считайте уникальные корни в конце, либо ведите счётчик: старт = n,
 *   при каждом union разных множеств уменьшайте на 1.
 */

public class Task04 {

    static int countComponents(int n, int[][] edges) {
        // TODO: DSU; union по рёбрам; вернуть число компонент
        return n;
    }

    public static void main(String[] args) {
        int[][] edges = {{0, 1}, {1, 2}, {3, 4}};
        System.out.println("компонент: " + countComponents(6, edges));   // 3
    }
}
