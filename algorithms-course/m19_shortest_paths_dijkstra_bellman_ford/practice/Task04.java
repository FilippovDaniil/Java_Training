package m19_shortest_paths_dijkstra_bellman_ford.practice;

/**
 * Задача 04 — Тема 19: Сравнить время на графах разных размеров
 *
 * ЗАДАНИЕ:
 *   Замерьте время работы Дейкстры (с PriorityQueue) на случайных графах
 *   возрастающего размера (например V = 1000, 2000, 4000 при E ≈ 5V).
 *   Покажите, что время растёт примерно как O((V+E) log V).
 *
 * ПРИМЕР / ВЫВОД (числа зависят от железа — важна тенденция):
 *   V=1000  E=5000   t= 3 ms
 *   V=2000  E=10000  t= 7 ms
 *   V=4000  E=20000  t=15 ms
 *   (рост близок к линейно-логарифмическому)
 *
 * ТРЕБОВАНИЯ:
 *   - генерировать случайные связные (или почти) графы с неотрицательными весами;
 *   - реализацию Дейкстры можно взять из задачи 01;
 *   - замерять System.nanoTime(); прогрев перед замером.
 *
 * ПОДСКАЗКА:
 *   Для генерации: на каждую вершину добавить несколько случайных рёбер со случайным весом.
 */

public class Task04 {

    static int[] dijkstra(int start, int[][][] adj) {
        // TODO: Дейкстра (adj[u] — массив рёбер {to, w}); вернуть dist
        return new int[adj.length];
    }

    public static void main(String[] args) {
        int[] sizes = {1000, 2000, 4000};
        // TODO: для каждого размера сгенерировать граф, замерить Дейкстру, вывести время
    }
}
