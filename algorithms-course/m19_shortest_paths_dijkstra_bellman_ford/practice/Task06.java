package m19_shortest_paths_dijkstra_bellman_ford.practice;

/**
 * Задача 06 — Тема 19: Самый дешёвый путь с ограничением на число рёбер
 *
 * ЗАДАНИЕ:
 *   Найдите минимальную стоимость пути от src до dst, использующего НЕ БОЛЕЕ
 *   k рёбер. Это вариация Беллмана-Форда: делаем ровно k итераций релаксации,
 *   но релаксируем из «снимка» предыдущей итерации (чтобы за одну итерацию не
 *   использовать более одного нового ребра).
 *
 * ПРИМЕР / ВЫВОД:
 *   рёбра: 0->1(100), 1->2(100), 0->2(500)
 *   cheapest(0, 2, k=1) -> 500   (прямое ребро, 1 ребро)
 *   cheapest(0, 2, k=2) -> 200   (0->1->2, 2 ребра, дешевле)
 *
 * ТРЕБОВАНИЯ:
 *   - граф ориентированный, веса неотрицательные;
 *   - не более k рёбер в пути;
 *   - использовать копию dist на каждой итерации (snapshot), иначе можно
 *     «проскочить» больше k рёбер за один проход;
 *   - если пути в пределах k рёбер нет — вернуть -1.
 *
 * ПОДСКАЗКА:
 *   Это классическая задача «cheapest flights within k stops» (k+1 ребро при k пересадках).
 */

public class Task06 {

    static int cheapest(int n, int[][] edges, int src, int dst, int k) {
        // TODO: k итераций релаксации по snapshot предыдущего dist; вернуть dist[dst] или -1
        return -1;
    }

    public static void main(String[] args) {
        int[][] edges = {{0,1,100},{1,2,100},{0,2,500}};
        System.out.println("k=1: " + cheapest(3, edges, 0, 2, 1));   // 500
        System.out.println("k=2: " + cheapest(3, edges, 0, 2, 2));   // 200
    }
}
