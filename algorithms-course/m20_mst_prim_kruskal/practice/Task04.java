package m20_mst_prim_kruskal.practice;

/**
 * Задача 04 — Тема 20: Второе по минимальности остовное дерево (идея)
 *
 * ЗАДАНИЕ:
 *   Найдите вес ВТОРОГО по минимальности остовного дерева — минимального среди
 *   всех остовных деревьев, ОТЛИЧНЫХ от MST хотя бы одним ребром.
 *   Идея (для небольших графов): построить MST; затем для каждого НЕ вошедшего
 *   в MST ребра (u,v,w) добавить его (образуется цикл), удалить максимальное
 *   ребро на пути u..v внутри MST, посчитать новый вес. Минимум таких замен и
 *   есть второе MST.
 *
 * ПРИМЕР / ВЫВОД:
 *   рёбра: (0-1,1),(1-2,2),(0-2,3)
 *   MST вес = 3 (рёбра 0-1,1-2); второе MST вес = 4 (заменить 1-2 на 0-2: 1+3)
 *
 * ТРЕБОВАНИЯ:
 *   - сначала MST (Крускал);
 *   - для каждого небазового ребра найти max ребро на пути между его концами в MST
 *     (можно простым DFS по дереву MST);
 *   - второе MST = min(weightMST - maxOnPath + w) по всем небазовым рёбрам;
 *   - достаточно работать на маленьких графах (перебор по дереву).
 *
 * ПОДСКАЗКА:
 *   Постройте MST как дерево смежности, затем DFS-ом ищите максимальное ребро на пути.
 */

public class Task04 {

    static int secondBestMst(int n, int[][] edges) {
        // TODO: построить MST; перебрать небазовые рёбра; для каждого найти max ребро
        //       на пути между концами в MST; вернуть минимальный вес второго MST
        return -1;
    }

    public static void main(String[] args) {
        int[][] edges = {{0,1,1},{1,2,2},{0,2,3}};
        System.out.println("второе MST вес = " + secondBestMst(3, edges));   // 4
    }
}
