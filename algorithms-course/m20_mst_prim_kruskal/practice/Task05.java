package m20_mst_prim_kruskal.practice;

/**
 * Задача 05 — Тема 20: Сравнить Крускал и Прим на разреженных и плотных графах
 *
 * ЗАДАНИЕ:
 *   Замерьте время Крускала (DSU) и Прима (куча) на двух типах графов одного
 *   размера: РАЗРЕЖЕННОМ (E ≈ V) и ПЛОТНОМ (E ≈ V²). Сделайте вывод, какой
 *   алгоритм где выгоднее.
 *
 * ПРИМЕР / ВЫВОД (числа зависят от железа — важна тенденция):
 *   разреженный V=5000 E~15000:  Крускал=.. ms  Прим=.. ms
 *   плотный     V=1000 E~500000: Крускал=.. ms  Прим=.. ms
 *   Вывод: на разреженных конкурентоспособны оба; на плотных сортировка рёбер
 *   делает Крускал тяжелее (E велико), Прим (по вершинам) часто выигрывает.
 *
 * ТРЕБОВАНИЯ:
 *   - генерировать связные графы обоих типов;
 *   - оба алгоритма должны давать ОДИНАКОВЫЙ вес MST (проверка корректности);
 *   - замерять время; прогрев.
 *
 * ПОДСКАЗКА:
 *   Реализации берите из задач 01 (Крускал) и 02 (Прим).
 */

public class Task05 {

    static int kruskal(int n, int[][] edges) {
        // TODO: из задачи 01
        return 0;
    }
    static int prim(int n, int[][][] adj) {
        // TODO: из задачи 02 (adj[u] — рёбра {to, w})
        return 0;
    }

    public static void main(String[] args) {
        // TODO: сгенерировать разреженный и плотный графы, замерить оба алгоритма,
        //       проверить совпадение веса MST, вывести времена и вывод
    }
}
