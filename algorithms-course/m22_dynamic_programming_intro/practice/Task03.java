package m22_dynamic_programming_intro.practice;

/**
 * Задача 03 — Тема 22: Рюкзак 0/1 (максимальная ценность)
 *
 * ЗАДАНИЕ:
 *   Дано n предметов с весами weight[i] и ценностями value[i], и рюкзак
 *   вместимостью W. Каждый предмет можно взять не более одного раза (0/1).
 *   Найдите максимальную суммарную ценность, помещающуюся в рюкзак.
 *
 * ПРИМЕР / ВЫВОД:
 *   веса=[1,3,4,5], ценности=[1,4,5,7], W=7
 *   максимальная ценность = 9   (предметы с весами 3 и 4: 4+5)
 *
 * ТРЕБОВАНИЯ:
 *   - dp[i][w] = макс ценность из первых i предметов при лимите веса w;
 *   - переход: не брать предмет i (dp[i-1][w]) или взять, если влезает
 *     (dp[i-1][w-weight] + value);
 *   - O(n·W); вернуть dp[n][W].
 *
 * ПОДСКАЗКА:
 *   Можно ужать до одномерного dp[w], идя по w от W вниз (чтобы не использовать
 *   предмет дважды), но сначала сделайте понятную 2D-версию.
 */

public class Task03 {

    static int knapsack(int[] weight, int[] value, int W) {
        // TODO: 2D DP dp[i][w]; вернуть dp[n][W]
        return 0;
    }

    public static void main(String[] args) {
        int[] weight = {1, 3, 4, 5};
        int[] value = {1, 4, 5, 7};
        System.out.println("макс ценность = " + knapsack(weight, value, 7));   // 9
    }
}
