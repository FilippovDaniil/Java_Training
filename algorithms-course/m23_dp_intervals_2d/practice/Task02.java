package m23_dp_intervals_2d.practice;

/**
 * Задача 02 — Тема 23: Умножение цепочки матриц
 *
 * ЗАДАНИЕ:
 *   Дана последовательность матриц, заданная размерами dims[], где i-я матрица
 *   имеет размер dims[i-1] x dims[i]. Найдите минимальное число скалярных
 *   умножений для перемножения всей цепочки (зависит от расстановки скобок).
 *
 * ПРИМЕР / ВЫВОД:
 *   dims=[10,20,30,40,30]  (4 матрицы: 10x20, 20x30, 30x40, 40x30)
 *   минимум умножений = 30000
 *   dims=[40,20,30,10,30] -> 26000
 *
 * ТРЕБОВАНИЯ:
 *   - interval DP: dp[l][r] = мин умножений для матриц l..r;
 *   - перебор точки разбиения k: dp[l][k] + dp[k+1][r] + dims[l-1]*dims[k]*dims[r];
 *   - заполнять по возрастанию длины отрезка; O(n³).
 *
 * ПОДСКАЗКА:
 *   База: dp[i][i]=0 (одна матрица). Внешний цикл — по длине цепочки.
 */

public class Task02 {

    static int matrixChainOrder(int[] dims) {
        // TODO: interval DP по возрастанию длины; вернуть dp[1][n-1]
        return 0;
    }

    public static void main(String[] args) {
        System.out.println(matrixChainOrder(new int[]{10, 20, 30, 40, 30}));   // 30000
        System.out.println(matrixChainOrder(new int[]{40, 20, 30, 10, 30}));   // 26000
    }
}
