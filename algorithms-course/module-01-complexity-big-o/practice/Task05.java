/**
 * Задача 05 — Тема 01: Найти и исправить O(n^3)
 *
 * ЗАДАНИЕ:
 *   Метод countTriples ниже считает количество троек индексов (i<j<k), для
 *   которых a[i]+a[j]+a[k] == 0. Сейчас он написан наивно за O(n^3).
 *   1) Подтвердите, что текущая версия — O(n^3) (комментарий).
 *   2) Напишите countTriplesFast, который решает ту же задачу быстрее
 *      (отсортировать + два указателя -> O(n^2)). Сложность укажите в TODO.
 *
 * ПРИМЕР / ВЫВОД:
 *   Вход: [-1, 0, 1, 2, -1, -4]
 *   Наивный:  кол-во троп с суммой 0 = 2
 *   Быстрый:  кол-во троп с суммой 0 = 2   (O(n^2))
 *
 * ТРЕБОВАНИЯ:
 *   - оба метода дают одинаковый ответ;
 *   - быстрый: отсортировать массив, зафиксировать i, двумя указателями
 *     (l=i+1, r=n-1) искать пары с нужной суммой;
 *   - аккуратно с дубликатами, если считаете уникальные тройки (по условию —
 *     считаем тройки ИНДЕКСОВ, дубликаты значений допустимы).
 *
 * ПОДСКАЗКА:
 *   После сортировки: если a[i]+a[l]+a[r] < 0 -> l++; если > 0 -> r--; если ==0 -> засчитать.
 */

import java.util.Arrays;

public class Task05 {

    // O(n^3) — наивная версия (можно использовать как эталон для проверки)
    static int countTriples(int[] a) {
        int n = a.length, c = 0;
        for (int i = 0; i < n; i++)
            for (int j = i + 1; j < n; j++)
                for (int k = j + 1; k < n; k++)
                    if (a[i] + a[j] + a[k] == 0) c++;
        return c;
    }

    // TODO: сложность? ___
    static int countTriplesFast(int[] a) {
        int[] s = a.clone();
        Arrays.sort(s);
        // TODO: для каждого i — два указателя l, r; считать тройки с суммой 0
        return 0;
    }

    public static void main(String[] args) {
        int[] data = {-1, 0, 1, 2, -1, -4};
        System.out.println("Наивный: " + countTriples(data));
        System.out.println("Быстрый: " + countTriplesFast(data));
    }
}
