/**
 * Задача 06 — Тема 02: removeDuplicates из отсортированного массива за O(n)
 *
 * ЗАДАНИЕ:
 *   В ОТСОРТИРОВАННОМ массиве удалите дубликаты НА МЕСТЕ за O(n), используя
 *   приём «медленный/быстрый указатель». Верните новую логическую длину
 *   (число уникальных элементов); первые k элементов массива должны быть
 *   уникальны и отсортированы.
 *
 * ПРИМЕР / ВЫВОД:
 *   Вход:  [1, 1, 2, 2, 2, 3, 4, 4]
 *   k = 4, начало массива: [1, 2, 3, 4, ...]
 *
 * ТРЕБОВАНИЯ:
 *   - вход отсортирован; работаем in-place, память O(1);
 *   - один проход (быстрый указатель), медленный отмечает позицию записи;
 *   - вернуть количество уникальных элементов k.
 *
 * ПОДСКАЗКА:
 *   slow=0; for fast=1..n-1: if (a[fast] != a[slow]) { slow++; a[slow]=a[fast]; }
 *   ответ k = slow + 1 (для непустого массива).
 */

import java.util.Arrays;

public class Task06 {

    static int removeDuplicates(int[] a) {
        // TODO: медленный/быстрый указатель; вернуть число уникальных
        return 0;
    }

    public static void main(String[] args) {
        int[] a = {1, 1, 2, 2, 2, 3, 4, 4};
        int k = removeDuplicates(a);
        System.out.println("k = " + k);
        System.out.println("начало: " + Arrays.toString(Arrays.copyOf(a, k)));
    }
}
