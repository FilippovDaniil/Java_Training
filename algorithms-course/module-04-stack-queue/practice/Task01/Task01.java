/**
 * Задача 01 — Тема 04: Стек на массиве (фиксированный + динамический)
 *
 * ЗАДАНИЕ:
 *   Реализуйте ArrayStack (файл ArrayStack.java) на массиве с операциями
 *   push, pop, peek, isEmpty, size. При заполнении массив удваивается
 *   (динамический рост). push/pop/peek — O(1) (push амортизированно).
 *
 * ПРИМЕР / ВЫВОД:
 *   push 1,2,3 -> size=3, peek=3
 *   pop -> 3, pop -> 2
 *   size=1
 *
 * ТРЕБОВАНИЯ:
 *   - НЕ использовать java.util.Stack/Deque — пишем свой на int[];
 *   - pop/peek на пустом -> исключение (например EmptyStackException или IllegalStateException);
 *   - продемонстрируйте рост за пределы начальной ёмкости.
 *
 * ПОДСКАЗКА:
 *   top — это и индекс вставки, и число элементов. Рост: новый массив вдвое, копирование.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: создать ArrayStack малой ёмкости, напушить больше неё (проверить рост),
        //       вывести peek/size, поппнуть несколько раз
    }
}
