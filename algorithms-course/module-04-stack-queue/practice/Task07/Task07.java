/**
 * Задача 07 (мини-проект Data-Cruncher) — Тема 04: очередь задач + RPN-калькулятор
 *
 * ЗАДАНИЕ:
 *   Добавьте в Data-Cruncher обработку очереди вычислительных задач, где
 *   каждая задача — выражение в обратной польской записи (RPN).
 *   1) RpnCalculator (файл RpnCalculator.java) — eval(String) считает RPN стеком.
 *   2) В main создайте ОЧЕРЕДЬ (FIFO) из нескольких RPN-выражений и обработайте
 *      их по порядку, печатая результат каждой задачи.
 *
 * ПРИМЕР / ВЫВОД:
 *   Задача "3 4 +"     = 7
 *   Задача "5 1 2 + 4 * + 3 -" = 14
 *   Задача "2 3 *"     = 6
 *
 * ТРЕБОВАНИЯ:
 *   - eval использует СТЕК; очередь задач — FIFO (ArrayDeque как очередь);
 *   - поддержите + - * / над целыми (long);
 *   - деление на ноль / некорректное выражение — осмысленная ошибка.
 *
 * ПОДСКАЗКА:
 *   Стек — внутри одного вычисления; очередь — для порядка задач. Это два разных применения.
 */

import java.util.ArrayDeque;
import java.util.Queue;

public class Task07 {
    public static void main(String[] args) {
        Queue<String> tasks = new ArrayDeque<>();
        tasks.offer("3 4 +");
        tasks.offer("5 1 2 + 4 * + 3 -");
        tasks.offer("2 3 *");
        RpnCalculator calc = new RpnCalculator();
        // TODO: пока очередь не пуста — poll выражение, посчитать eval, напечатать результат
    }
}
