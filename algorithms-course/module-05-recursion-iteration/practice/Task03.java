/**
 * Задача 03 — Тема 05: Ханойские башни
 *
 * ЗАДАНИЕ:
 *   Выведите последовательность перемещений для переноса n дисков со стержня
 *   'A' на стержень 'C', используя 'B' как вспомогательный. Классическая
 *   рекурсия: перенести (n-1) на вспомогательный, переложить большой диск,
 *   перенести (n-1) поверх него.
 *
 * ПРИМЕР / ВЫВОД (n=3):
 *   Переместить диск 1: A -> C
 *   Переместить диск 2: A -> B
 *   Переместить диск 1: C -> B
 *   Переместить диск 3: A -> C
 *   Переместить диск 1: B -> A
 *   Переместить диск 2: B -> C
 *   Переместить диск 1: A -> C
 *   Всего перемещений: 7  (= 2^n - 1)
 *
 * ТРЕБОВАНИЯ:
 *   - вывести каждое перемещение; число ходов = 2^n - 1;
 *   - посчитать и вывести общее число перемещений;
 *   - параметры: from, to, aux (исходный, целевой, вспомогательный стержни).
 *
 * ПОДСКАЗКА:
 *   hanoi(n, from, to, aux): if(n==0) return;
 *   hanoi(n-1, from, aux, to); печать хода диска n; hanoi(n-1, aux, to, from);
 */

public class Task03 {

    static int moves = 0;

    static void hanoi(int n, char from, char to, char aux) {
        // TODO: рекурсивно; на ходе диска n печатать "Переместить диск n: from -> to" и moves++
    }

    public static void main(String[] args) {
        int n = 3;
        moves = 0;
        hanoi(n, 'A', 'C', 'B');
        System.out.println("Всего перемещений: " + moves);
    }
}
