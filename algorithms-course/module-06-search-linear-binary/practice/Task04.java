/**
 * Задача 04 — Тема 06: Поиск в сдвинутом отсортированном массиве (rotated)
 *
 * ЗАДАНИЕ:
 *   Массив был отсортирован по возрастанию, затем циклически сдвинут
 *   (например [0,1,2,4,5,6,7] -> [4,5,6,7,0,1,2]). Найдите индекс target
 *   за O(log n), используя модифицированный бинарный поиск.
 *
 * ПРИМЕР / ВЫВОД:
 *   a = [4, 5, 6, 7, 0, 1, 2]
 *   search(0) -> 4
 *   search(6) -> 2
 *   search(3) -> -1
 *
 * ТРЕБОВАНИЯ:
 *   - O(log n), без «выпрямления» массива;
 *   - дубликатов нет;
 *   - идея: одна из половин [lo..mid] или [mid..hi] всегда отсортирована —
 *     определите какая и попадает ли target в её диапазон.
 *
 * ПОДСКАЗКА:
 *   Если a[lo] <= a[mid] — левая половина отсортирована: если target в [a[lo], a[mid])
 *   -> искать слева, иначе справа. Симметрично для правой половины.
 */

public class Task04 {

    static int search(int[] a, int target) {
        // TODO: модифицированный бинпоиск для rotated-массива
        return -1;
    }

    public static void main(String[] args) {
        int[] a = {4, 5, 6, 7, 0, 1, 2};
        System.out.println("search(0) = " + search(a, 0));
        System.out.println("search(6) = " + search(a, 6));
        System.out.println("search(3) = " + search(a, 3));
    }
}
