/**
 * Задача 01 — Тема 07: Пузырьковая сортировка с оптимизацией
 *
 * ЗАДАНИЕ:
 *   Реализуйте пузырьковую сортировку по возрастанию с РАННИМ ВЫХОДОМ:
 *   если за очередной полный проход не было ни одного обмена — массив
 *   уже отсортирован, прекращаем работу.
 *
 * ПРИМЕР / ВЫВОД:
 *   До:    [5, 3, 8, 1, 2]
 *   После: [1, 2, 3, 5, 8]
 *   На уже отсортированном массиве — один проход (ранний выход).
 *
 * ТРЕБОВАНИЯ:
 *   - сортировка in-place;
 *   - флаг swapped для раннего выхода;
 *   - внутренний цикл сокращается: после i проходов последние i элементов на месте.
 *
 * ПОДСКАЗКА:
 *   for i: swapped=false; for j in [0, n-1-i): если a[j]>a[j+1] — swap, swapped=true;
 *   если !swapped — break.
 */

import java.util.Arrays;

public class Task01 {

    static void bubbleSort(int[] a) {
        // TODO: пузырёк с ранним выходом
    }

    public static void main(String[] args) {
        int[] a = {5, 3, 8, 1, 2};
        bubbleSort(a);
        System.out.println(Arrays.toString(a));
    }
}
