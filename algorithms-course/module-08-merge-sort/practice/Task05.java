/**
 * Задача 05 — Тема 08: Подсчёт числа инверсий (модификация merge sort)
 *
 * ЗАДАНИЕ:
 *   Инверсия — пара индексов (i < j), для которых a[i] > a[j]. Их количество —
 *   мера «неотсортированности» массива. Посчитайте число инверсий за O(n log n),
 *   доработав merge sort: при слиянии, когда элемент из ПРАВОЙ части меньше
 *   элемента из левой, он образует инверсии со ВСЕМИ оставшимися в левой части.
 *
 * ПРИМЕР / ВЫВОД:
 *   [2, 4, 1, 3, 5] -> инверсий 3   (пары: (2,1),(4,1),(4,3))
 *   [5, 4, 3, 2, 1] -> инверсий 10  (полностью обратный — максимум)
 *   [1, 2, 3]       -> инверсий 0
 *
 * ТРЕБОВАНИЯ:
 *   - O(n log n) (наивный подсчёт за O(n²) не принимается);
 *   - массив можно модифицировать (он отсортируется попутно);
 *   - при слиянии: взяли a[j] из правой части -> инверсий += (mid - i + 1).
 *
 * ПОДСКАЗКА:
 *   countAndSort(a, lo, hi) возвращает число инверсий и сортирует a[lo..hi].
 *   Итог = инверсии слева + справа + при слиянии.
 */

public class Task05 {

    static long countInversions(int[] a) {
        // TODO: запустить рекурсивный countAndSort(a, 0, a.length-1)
        return 0;
    }

    // TODO: вспомогательные countAndSort(...) и mergeCount(...)

    public static void main(String[] args) {
        System.out.println(countInversions(new int[]{2, 4, 1, 3, 5}));   // 3
        System.out.println(countInversions(new int[]{5, 4, 3, 2, 1}));   // 10
        System.out.println(countInversions(new int[]{1, 2, 3}));         // 0
    }
}
