/**
 * Задача 03 — Тема 19: Беллман-Форд + поиск отрицательного цикла
 *
 * ЗАДАНИЕ:
 *   Реализуйте Беллмана-Форда для ОРиентированного графа с ЛЮБЫМИ весами:
 *   V-1 итераций релаксации всех рёбер -> кратчайшие расстояния от source.
 *   Дополнительная V-я итерация: если что-то ещё улучшается — есть
 *   отрицательный цикл (верните соответствующий признак).
 *
 * ПРИМЕР / ВЫВОД:
 *   граф без отриц. цикла: 0->1(4), 0->2(5), 1->2(-3), 2->3(2)
 *   dist от 0: [0, 4, 1, 3]
 *   граф с отриц. циклом: 0->1(1), 1->2(-1), 2->0(-1)
 *   hasNegativeCycle: true
 *
 * ТРЕБОВАНИЯ:
 *   - рёбра — список {u, v, w}; dist инициализировать INF, dist[source]=0;
 *   - V-1 проходов по всем рёбрам с релаксацией (проверяя dist[u] != INF);
 *   - V-я итерация выявляет отрицательный цикл;
 *   - метод возвращает либо dist, либо признак отрицательного цикла.
 *
 * ПОДСКАЗКА:
 *   Если на V-й итерации хоть одно ребро релаксируется -> цикл с отрицательным весом.
 */

public class Task03 {

    /** Возвращает dist[], либо null, если найден отрицательный цикл. */
    static int[] bellmanFord(int n, int[][] edges, int source) {
        int[] dist = new int[n];
        java.util.Arrays.fill(dist, Integer.MAX_VALUE);
        dist[source] = 0;
        // TODO: V-1 итераций релаксации; V-я итерация -> проверка отрицательного цикла (вернуть null)
        return dist;
    }

    public static void main(String[] args) {
        int[][] g1 = {{0,1,4},{0,2,5},{1,2,-3},{2,3,2}};
        System.out.println("dist: " + java.util.Arrays.toString(bellmanFord(4, g1, 0)));
        int[][] g2 = {{0,1,1},{1,2,-1},{2,0,-1}};
        System.out.println("отрицательный цикл? -> " + (bellmanFord(3, g2, 0) == null));
    }
}
