/**
 * Задача 06 — Тема 20: Соединить города минимальной стоимостью
 *
 * ЗАДАНИЕ:
 *   Дано n городов и список возможных дорог (u, v, cost). Найдите минимальную
 *   суммарную стоимость, чтобы ВСЕ города были связаны (прямо или через другие).
 *   Если связать все города невозможно — верните -1. Это прямое применение MST.
 *
 * ПРИМЕР / ВЫВОД:
 *   n=4, дороги: (0-1,1),(1-2,2),(2-3,3),(0-3,10)
 *   минимальная стоимость = 6   (рёбра 1+2+3)
 *   если граф несвязный -> -1
 *
 * ТРЕБОВАНИЯ:
 *   - MST через Крускал; суммировать веса выбранных рёбер;
 *   - если после обработки всех рёбер использовано < n-1 рёбер -> граф несвязный -> -1;
 *   - стоимости положительные.
 *
 * ПОДСКАЗКА:
 *   Считайте число успешных union: если оно == n-1 — связали все; иначе -1.
 */

public class Task06 {

    static int minCostToConnect(int n, int[][] roads) {
        // TODO: Крускал; вернуть суммарную стоимость или -1, если связать нельзя
        return -1;
    }

    public static void main(String[] args) {
        int[][] roads = {{0,1,1},{1,2,2},{2,3,3},{0,3,10}};
        System.out.println("стоимость = " + minCostToConnect(4, roads));   // 6
        int[][] disconnected = {{0,1,1}};   // вершины 2,3 недостижимы
        System.out.println("несвязный = " + minCostToConnect(4, disconnected));   // -1
    }
}
