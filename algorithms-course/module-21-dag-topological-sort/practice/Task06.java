/**
 * Задача 06 — Тема 21: Конденсация компонент сильной связности (упрощённо)
 *
 * ЗАДАНИЕ:
 *   Любой орграф можно «схлопнуть» в DAG, объединив компоненты сильной связности
 *   (SCC) в супервершины. Здесь — упрощённый вариант: SCC находите ЛЮБЫМ способом
 *   (например двойной DFS — Косарайю: DFS на графе для порядка, затем DFS на
 *   транспонированном в этом порядке). Верните число SCC и метку компоненты
 *   каждой вершины.
 *
 * ПРИМЕР / ВЫВОД:
 *   0->1, 1->2, 2->0 (это одна SCC), 2->3, 3->4, 4->3 (вторая SCC {3,4})
 *   число SCC: 2   (компоненты {0,1,2} и {3,4})
 *
 * ТРЕБОВАНИЯ:
 *   - найти SCC (Косарайю: 1) DFS, складывая вершины в стек по завершении;
 *     2) транспонировать граф; 3) DFS в порядке стека — каждый запуск = одна SCC);
 *   - вернуть количество SCC и/или метки;
 *   - конденсация (граф супервершин) — DAG (можно упомянуть, строить не обязательно).
 *
 * ПОДСКАЗКА:
 *   Транспонирование графа — из темы 17, задача 05. Косарайю = 2 прохода DFS.
 */

import java.util.ArrayList;
import java.util.List;

public class Task06 {

    static int countSCC(int n, List<List<Integer>> adj) {
        // TODO: Косарайю — порядок завершения DFS; DFS по транспонированному графу;
        //       число запусков второго DFS = число SCC
        return 0;
    }

    static List<List<Integer>> build(int n, int[][] edges) {
        List<List<Integer>> adj = new ArrayList<>();
        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());
        for (int[] e : edges) adj.get(e[0]).add(e[1]);
        return adj;
    }

    public static void main(String[] args) {
        var adj = build(5, new int[][]{{0,1},{1,2},{2,0},{2,3},{3,4},{4,3}});
        System.out.println("число SCC = " + countSCC(5, adj));   // 2
    }
}
