/**
 * Задача 02 — Тема 22: Подъём по лестнице (1 или 2 шага)
 *
 * ЗАДАНИЕ:
 *   Сколькими способами можно подняться на n ступеней, делая шаги по 1 или 2
 *   ступени за раз? Это Фибоначчи в маскировке: dp[i] = dp[i-1] + dp[i-2].
 *
 * ПРИМЕР / ВЫВОД:
 *   n=2 -> 2   (1+1, 2)
 *   n=3 -> 3   (1+1+1, 1+2, 2+1)
 *   n=5 -> 8
 *
 * ТРЕБОВАНИЯ:
 *   - DP (табуляция); база dp[0]=1 (один способ «стоять на месте»), dp[1]=1;
 *   - O(n) время, можно O(1) память;
 *   - использовать long.
 *
 * ПОДСКАЗКА:
 *   Чтобы попасть на ступень i, приходим с i-1 (шаг 1) или с i-2 (шаг 2).
 */

public class Task02 {

    static long climbStairs(int n) {
        // TODO: dp[i] = dp[i-1] + dp[i-2], база dp[0]=dp[1]=1
        return 0;
    }

    public static void main(String[] args) {
        System.out.println("n=2 -> " + climbStairs(2));
        System.out.println("n=3 -> " + climbStairs(3));
        System.out.println("n=5 -> " + climbStairs(5));
    }
}
