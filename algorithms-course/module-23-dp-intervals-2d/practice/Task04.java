/**
 * Задача 04 — Тема 23: Задача о треугольнике (минимальный путь)
 *
 * ЗАДАНИЕ:
 *   Дан треугольник чисел (строка i содержит i+1 элементов). Спускаясь от
 *   вершины к основанию (из позиции j можно шагнуть в j или j+1 на след. строке),
 *   найдите путь с минимальной суммой.
 *
 * ПРИМЕР / ВЫВОД:
 *   [[2],[3,4],[6,5,7],[4,1,8,3]]
 *   минимальная сумма = 11   (2 + 3 + 5 + 1)
 *
 * ТРЕБОВАНИЯ:
 *   - удобно считать СНИЗУ ВВЕРХ: dp по последней строке = сами значения;
 *     dp[j] = triangle[i][j] + min(dp[j], dp[j+1]);
 *   - в конце dp[0] — ответ; O(n²) по числу элементов;
 *   - можно и сверху вниз — на ваш выбор.
 *
 * ПОДСКАЗКА:
 *   Снизу вверх элегантнее: на каждой строке dp ужимается на 1 элемент.
 */

import java.util.Arrays;
import java.util.List;

public class Task04 {

    static int minPath(List<int[]> triangle) {
        // TODO: DP снизу вверх; вернуть минимальную сумму пути от вершины
        return 0;
    }

    public static void main(String[] args) {
        List<int[]> t = List.of(
            new int[]{2},
            new int[]{3, 4},
            new int[]{6, 5, 7},
            new int[]{4, 1, 8, 3}
        );
        System.out.println("мин сумма = " + minPath(t));   // 11
    }
}
