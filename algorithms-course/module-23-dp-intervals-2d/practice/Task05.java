/**
 * Задача 05 — Тема 23: Уникальные пути в сетке с препятствиями
 *
 * ЗАДАНИЕ:
 *   В сетке m×n робот идёт из левого верхнего угла в правый нижний, двигаясь
 *   только вправо или вниз. Некоторые клетки — препятствия (1). Посчитайте
 *   число различных путей. Если старт или финиш — препятствие, путей 0.
 *
 * ПРИМЕР / ВЫВОД:
 *   grid:
 *     0 0 0
 *     0 1 0
 *     0 0 0
 *   уникальных путей = 2
 *
 * ТРЕБОВАНИЯ:
 *   - dp[i][j] = число путей в (i,j); препятствие -> dp=0;
 *   - dp[i][j] = dp[i-1][j] + dp[i][j-1] (если клетка свободна);
 *   - аккуратная инициализация первой строки/столбца (обрыв на препятствии);
 *   - вернуть dp[m-1][n-1].
 *
 * ПОДСКАЗКА:
 *   В первой строке/столбце путь существует только до первого препятствия.
 */

public class Task05 {

    static int uniquePaths(int[][] grid) {
        // TODO: 2D DP с учётом препятствий
        return 0;
    }

    public static void main(String[] args) {
        int[][] grid = {
            {0, 0, 0},
            {0, 1, 0},
            {0, 0, 0}
        };
        System.out.println("путей = " + uniquePaths(grid));   // 2
    }
}
