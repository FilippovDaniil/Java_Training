/**
 * Модуль Data-Cruncher: анализ последовательностей.
 *  - longestIncreasing — длина наибольшей возрастающей подпоследовательности (тренд);
 *  - longestPalindrome — самая длинная палиндромная подстрока в тексте.
 * Применение: выявление трендов в числовых рядах и симметрий в текстовых данных.
 */
public class SequenceAnalyzer {

    /** Длина LIS числового ряда (O(n log n)). */
    public int longestIncreasing(int[] data) {
        // TODO: tails + бинарный поиск (как в задаче 01)
        return 0;
    }

    /** Самая длинная палиндромная подстрока (DP O(n²)). */
    public String longestPalindrome(String s) {
        // TODO: 2D DP (как в задаче 03)
        return "";
    }
}
