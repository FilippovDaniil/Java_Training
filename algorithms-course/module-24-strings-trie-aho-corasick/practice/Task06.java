/**
 * Задача 06 — Тема 24: Сравнить наивный поиск множества паттернов и Ахо-Корасик
 *
 * ЗАДАНИЕ:
 *   Сравните два подхода к поиску НЕСКОЛЬКИХ паттернов в тексте:
 *     - наивный: для каждого паттерна отдельный проход (например indexOf в цикле);
 *     - Ахо-Корасик: один проход по тексту для всех паттернов сразу.
 *   Замерьте время/число операций на большом тексте и многих паттернах.
 *
 * ПРИМЕР / ВЫВОД (числа зависят от данных — важна тенденция):
 *   текст ~200000 символов, 50 паттернов
 *   наивный:     t= 95 ms  (≈ паттернов × длина текста)
 *   Ахо-Корасик: t=  7 ms  (один проход)
 *   Оба находят одинаковое число совпадений.
 *
 * ТРЕБОВАНИЯ:
 *   - оба метода находят ОДИНАКОВОЕ число совпадений (проверка корректности);
 *   - наивный: цикл по паттернам, в каждом — поиск всех вхождений;
 *   - Ахо-Корасик: построить автомат один раз, один проход по тексту;
 *   - вывести время/счётчики и вывод.
 *
 * ПОДСКАЗКА:
 *   Ахо-Корасик выигрывает тем сильнее, чем больше паттернов и длиннее текст.
 */

import java.util.List;

public class Task06 {

    static int naiveCount(String text, List<String> patterns) {
        // TODO: для каждого паттерна найти все вхождения, суммировать
        return 0;
    }

    static int ahoCorasickCount(String text, List<String> patterns) {
        // TODO: построить автомат (бор + fail), один проход, суммировать совпадения
        return 0;
    }

    public static void main(String[] args) {
        // TODO: построить большой текст и набор паттернов; сравнить счётчики и время
    }
}
