package m05_loops.practice;

/**
 * Задача 04 — Модуль 05: Валидация ввода (do-while)
 *
 * ЗАДАНИЕ:
 *   Используя цикл do-while, требуйте у пользователя ввести
 *   положительное число. Пока введено число <= 0, повторяйте запрос.
 *   После корректного ввода выведите "Принято: <число>".
 *
 * ПРИМЕР:
 *   Ввод: -5, 0, 7 → (повтор запроса дважды) → Принято: 7
 *
 * ПОДСКАЗКА:
 *   do { читаем число } while (число <= 0);
 */
import java.util.Scanner;

public class Task04 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        // Ваш код здесь

        scanner.close();
    }
}
