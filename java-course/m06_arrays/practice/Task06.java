package m06_arrays.practice;

/**
 * Задача 06 — Модуль 06: Линейный поиск
 *
 * ЗАДАНИЕ:
 *   Считайте с клавиатуры искомое число. Найдите его индекс в массиве.
 *   Если число есть — выведите его индекс, иначе выведите -1.
 *
 * ПРИМЕР (массив {5, 8, 12, 3, 9}):
 *   Ввод: 12 → Индекс: 2
 *   Ввод: 7  → Индекс: -1
 *
 * ПОДСКАЗКА:
 *   Перебирайте массив; при совпадении запомните индекс и прервите
 *   цикл (break). Если не нашли — результат остаётся -1.
 */
import java.util.Scanner;

public class Task06 {
    public static void main(String[] args) {
        int[] a = {5, 8, 12, 3, 9};
        Scanner scanner = new Scanner(System.in);
        int target = scanner.nextInt();
        // Ваш код здесь

        scanner.close();
    }
}
