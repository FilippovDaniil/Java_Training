package m122_kafka_serialization_contracts.practice;

/**
 * Задача 06 — Модуль 122: разбор совместимости — восемь изменений схемы
 *
 * ФОРМАТ: аналитическая задача-носитель (.java + text-блок с таблицей-ответом + println,
 *   компилируется bare-javac). Проверка — по критериям ниже.
 *
 * ЗАДАНИЕ:
 *   Дана схема события task.created v1:
 *     { taskId: long, title: string, authorId: long }
 *   Для каждого изменения определите: совместимо ли оно BACKWARD (новый код читает старые данные),
 *   FORWARD (старый код читает новые данные), и что нужно сделать, чтобы выпустить его безопасно.
 *   Заполните таблицу в ANSWER:
 *     1) добавить поле priority: string с default "NORMAL"
 *     2) добавить поле assigneeId: long БЕЗ дефолта
 *     3) удалить поле authorId
 *     4) переименовать title -> name
 *     5) сменить тип taskId: long -> string
 *     6) сменить тип taskId: int -> long (если бы в v1 был int)
 *     7) в поле status (enum NEW, DONE) добавить значение CANCELLED
 *     8) оставить поле priority, но начать писать в него значения "1","2","3" вместо "LOW","NORMAL"
 *
 * ФОРМАТ ОТВЕТА (таблица в ANSWER):
 *   | # | Изменение | BACKWARD | FORWARD | Как выпускать безопасно |
 *
 * КРИТЕРИИ ПРИЁМКИ:
 *   - для каждого пункта указаны оба режима (да/нет) и порядок выпуска (кто обновляется первым);
 *   - пункт 8 отмечен как САМЫЙ опасный: схема формально не меняется, проверка Registry
 *     проходит, а потребители ломаются на данных (семантическая несовместимость);
 *   - для ломающих изменений (4, 5, частично 3) указан путь: новый топик .v2 + двойная
 *     публикация + вывод v1 из эксплуатации;
 *   - для пункта 2 указано: обязательное поле без дефолта ломает BACKWARD -> дать дефолт.
 *
 * ЦЕЛЬ: научиться оценивать изменение контракта ДО релиза и выбирать порядок выпуска
 *       (сначала потребители или сначала производители).
 *
 * ПОДСКАЗКА: правило порядка выпуска — при BACKWARD-изменении первыми обновляют потребителей,
 *   при FORWARD-изменении первыми обновляют производителей. Таблица режимов — в theory.md.
 */
public class Task06 {
    public static void main(String[] args) {
        String answer = """
                # TODO: заполните таблицу разбора совместимости
                # | # | Изменение                                | BACKWARD | FORWARD | Как выпускать безопасно |
                # |---|------------------------------------------|----------|---------|-------------------------|
                # | 1 | +priority (default "NORMAL")             |    ?     |    ?    |                         |
                # | 2 | +assigneeId (без дефолта)                |    ?     |    ?    |                         |
                # | 3 | -authorId                                |    ?     |    ?    |                         |
                # | 4 | title -> name                            |    ?     |    ?    |                         |
                # | 5 | taskId: long -> string                   |    ?     |    ?    |                         |
                # | 6 | taskId: int -> long                      |    ?     |    ?    |                         |
                # | 7 | enum status += CANCELLED                 |    ?     |    ?    |                         |
                # | 8 | смысл priority: "LOW/NORMAL" -> "1/2/3"  |    ?     |    ?    |                         |
                #
                # ВЫВОД (2-3 строки): какие изменения делать нельзя никогда и почему пункт 8
                # опаснее пункта 4.
                """;
        System.out.println(answer);
    }
}
