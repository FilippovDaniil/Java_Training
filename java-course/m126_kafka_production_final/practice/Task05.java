package m126_kafka_production_final.practice;

/**
 * Задача 05 — Модуль 126: расчёт ёмкости и тюнинг под три профиля нагрузки
 *
 * ФОРМАТ: аналитическая задача-носитель (.java + text-блок с расчётами и таблицей + println).
 *
 * ЗАДАНИЕ:
 *   Для трёх профилей посчитайте партиции, диск и подберите параметры продюсера/потребителя.
 *
 *   ПРОФИЛЬ A — «уведомления» (Task Tracker):
 *     200 событий/с в пике, размер события 0.5 КБ, retention 7 дней, RF=3,
 *     обработка одного события ~20 мс (внешний SMTP), важен порядок по задаче.
 *   ПРОФИЛЬ B — «аналитика кликов»:
 *     20 000 событий/с, 0.3 КБ, retention 3 дня, RF=3, обработка батчами по 500 в БД,
 *     порядок не важен, задержка до минуты допустима.
 *   ПРОФИЛЬ C — «платежи»:
 *     50 событий/с, 1 КБ, retention 30 дней, RF=3, обработка ~200 мс (внешний шлюз),
 *     потеря недопустима, дубликаты недопустимы, строгий порядок по счёту.
 *
 *   Для каждого профиля посчитайте и заполните таблицу в ANSWER:
 *     1) требуемое число консьюмеров: пропускная_способность / (1000 мс / время_обработки);
 *     2) число партиций (с запасом на рост, но не «на всякий случай тысячу»);
 *     3) объём диска: события/с × размер × retention_сек × RF × 1.3;
 *     4) параметры продюсера: acks, enable.idempotence, linger.ms, batch.size, compression.type;
 *     5) параметры потребителя: max.poll.records, concurrency, ack-mode, isolation.level;
 *     6) стратегию ошибок: blocking retry или @RetryableTopic, DLQ, дедупликация;
 *     7) нужен ли outbox (и почему).
 *
 * ФОРМАТ ОТВЕТА: таблица «Профиль | Консьюмеры | Партиции | Диск | Продюсер | Потребитель |
 *   Ошибки | Outbox» + 3-4 строки обоснования по каждому профилю.
 *
 * КРИТЕРИИ ПРИЁМКИ:
 *   - расчёты приведены с формулами и единицами измерения (не «примерно много»);
 *   - для профиля B выбраны батчевое чтение и сжатие, порядок явно признан неважным;
 *   - для профиля C выбраны acks=all, идемпотентность, blocking retry (порядок!), outbox,
 *     дедупликация по eventId; неблокирующие ретраи ОТВЕРГНУТЫ с обоснованием;
 *   - для профиля A показано, почему concurrency ограничен числом партиций, а не «побольше»;
 *   - в расчёте диска учтён RF и запас; при большом объёме предложено уменьшить retention
 *     или включить сжатие/compaction.
 *
 * ЦЕЛЬ: научиться выводить конфигурацию из требований, а не копировать её из чужого блога.
 *
 * ПОДСКАЗКА: считайте «сколько партиций нужно» от двух ограничений сразу — пропускной
 *   способности одного консьюмера и желаемого числа экземпляров сервиса. Партиций должно быть
 *   не меньше максимума из этих двух чисел.
 */
public class Task05 {
    public static void main(String[] args) {
        String answer = """
                # TODO: расчёты и тюнинг по трём профилям
                #
                # | Профиль | Консьюмеры | Партиции | Диск | Продюсер | Потребитель | Ошибки | Outbox |
                # |---------|-----------|----------|------|----------|-------------|--------|--------|
                # | A уведомления (200/с, 20 мс) |  |  |  |  |  |  |  |
                # | B аналитика (20000/с, батчи) |  |  |  |  |  |  |  |
                # | C платежи (50/с, 200 мс)     |  |  |  |  |  |  |  |
                #
                # РАСЧЁТЫ (с формулами):
                # A: консьюмеры = 200 / (1000/20) = 200/50 = 4 -> партиций >= ...
                #    диск = 200 × 0.5 КБ × 604800 × 3 × 1.3 = ... ГБ
                # B: ...
                # C: ...
                #
                # ОБОСНОВАНИЯ (3-4 строки на профиль):
                # A: ...
                # B: ...
                # C: почему blocking retry, а не @RetryableTopic; почему нужен outbox; ...
                """;
        System.out.println(answer);
    }
}
