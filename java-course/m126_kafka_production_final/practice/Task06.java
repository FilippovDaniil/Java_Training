package m126_kafka_production_final.practice;

/**
 * Задача 06 — Модуль 126: runbook инцидентов событийного сервиса
 *
 * ФОРМАТ: аналитическая задача-носитель (.java + text-блок с runbook + println, bare-javac).
 *
 * ЗАДАНИЕ:
 *   Составьте runbook — документ, по которому дежурный разбирает инцидент, не имея контекста.
 *   Для каждого симптома опишите: 1) какие метрики/команды смотреть, 2) вероятные причины,
 *   3) действия по порядку, 4) как проверить, что помогло, 5) что записать в постмортем.
 *
 *   СИМПТОМЫ (все обязательны):
 *     1) lag группы notifier растёт линейно уже 20 минут;
 *     2) lag «пилой»: то падает, то растёт; в логах повторяется Rebalancing;
 *     3) в task.created.DLT за 10 минут появилось 300 записей;
 *     4) продюсер получает TimeoutException: Topic not present in metadata;
 *     5) продюсер получает NOT_ENOUGH_REPLICAS;
 *     6) пользователи получили по два письма об одной задаче;
 *     7) в проекции дашборда данные «отстают» на часы;
 *     8) диск брокера заполнен на 92%;
 *     9) после деплоя приложение в CrashLoopBackOff (K8s);
 *    10) потребитель не получает события, хотя продюсер публикует (LAG = 0!).
 *
 *   Для каждого пункта приведите КОНКРЕТНЫЕ команды, например:
 *     kafka-consumer-groups.sh --bootstrap-server kafka:9092 --describe --group notifier
 *     kubectl logs deployment/app -n task-tracker --tail=200
 *     kafka-topics.sh --describe --topic task.created         # Isr, Leader
 *     kafka-log-dirs.sh --bootstrap-server kafka:9092 --describe   # размеры логов
 *
 * КРИТЕРИИ ПРИЁМКИ:
 *   - в п.2 названы max.poll.interval.ms / max-poll-records и длительность ретраев;
 *   - в п.6 причина — отсутствие дедупликации по eventId, а не «Kafka присылает дубли»;
 *   - в п.9 упомянуты enableServiceLinks: false и initContainer wait-for-kafka;
 *   - в п.10 названы правильные причины при нулевом lag: другой group.id, другой топик,
 *     слушатель на паузе/не запущен, auto-offset-reset=latest на новой группе,
 *     потребитель читает не тот кластер;
 *   - в п.3 есть процедура: разобрать заголовки kafka_dlt-*, исправить причину, переиграть DLT;
 *   - для каждого симптома указано, ЧЕМ подтверждается устранение (метрика вернулась к норме).
 *
 * ЦЕЛЬ: превратить знания части 5 в эксплуатационный документ — именно он отличает сервис,
 *       который «поддерживают», от сервиса, который «чинит тот, кто его писал».
 *
 * ПОДСКАЗКА: runbook хранится в репозитории сервиса рядом с кодом и обновляется после каждого
 *   инцидента: новый симптом -> новая строка. Таблица подводных камней в theory.md — заготовка.
 */
public class Task06 {
    public static void main(String[] args) {
        String runbook = """
                # TODO: RUNBOOK событийного сервиса Task Tracker / notifier
                #
                # ## 1. Lag растёт линейно
                # Смотреть:   kafka-consumer-groups.sh ... --describe --group notifier ; метрика p99 обработки
                # Причины:    ...
                # Действия:   1) ... 2) ... 3) ...
                # Проверка:   lag снижается, скорость обработки >= входящего потока
                # Постмортем: ...
                #
                # ## 2. Lag «пилой» + Rebalancing
                # ...
                # ## 3. 300 записей в DLT за 10 минут
                # ...
                # ## 4. TimeoutException: Topic not present in metadata
                # ## 5. NOT_ENOUGH_REPLICAS
                # ## 6. Двойные письма
                # ## 7. Проекция отстаёт на часы
                # ## 8. Диск брокера 92%
                # ## 9. CrashLoopBackOff после деплоя
                # ## 10. Потребитель ничего не получает при LAG = 0
                """;
        System.out.println(runbook);
    }
}
