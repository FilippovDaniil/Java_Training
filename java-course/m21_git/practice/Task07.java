package m21_git.practice;

/**
 * Задача 07 — Модуль 21 (МИНИ-ПРОЕКТ): Полный цикл разработки в Git
 *
 * ЗАДАНИЕ:
 *   Пройдите полный жизненный цикл небольшого проекта в Git, имитируя
 *   реальную работу над фичей с откатом ошибки.
 *
 *   1. Создайте новый репозиторий project-app и сделайте начальный
 *      коммит с файлом README.md ("# My App").
 *   2. Заведите ветку feature-calc, в ней добавьте файл calc.txt
 *      ("сложение, вычитание"), закоммитьте.
 *   3. Вернитесь в main, заведите ветку feature-docs, добавьте
 *      docs.txt ("документация"), закоммитьте.
 *   4. Слейте обе ветки в main по очереди.
 *   5. Сделайте «ошибочный» коммит (например, удалите README.md и
 *      закоммитьте), затем ОТМЕНИТЕ его через git revert.
 *   6. Выведите итоговую историю: git log --oneline --graph --all.
 *
 * ПРОВЕРКА (что вы должны увидеть):
 *   - в main присутствуют README.md, calc.txt, docs.txt;
 *   - в истории есть два merge-коммита и revert-коммит;
 *   - граф показывает ответвления и слияния.
 *
 * ПОДСКАЗКА:
 *   Это закрепление всех команд модуля: init, add, commit, branch,
 *   switch/checkout, merge, revert, log. Выполняйте по шагам и после
 *   каждого проверяйте git status / git log.
 */
public class Task07 {
    public static void main(String[] args) {
        System.out.println("Выполните задание командами Git в терминале (см. комментарий выше).");
    }
}
