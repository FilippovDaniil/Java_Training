package m22_oop_encapsulation_polymorphism_interfaces.practice.task05;

/**
 * Задача 05 — Модуль 22: Агрегация (HAS-A, слабая связь)
 *
 * ЗАДАНИЕ:
 *   Смоделируйте агрегацию: класс Team содержит список игроков Player,
 *   которые создаются СНАРУЖИ и передаются в команду.
 *   1. Класс Player (name).
 *   2. Класс Team (name + List<Player>): конструктор принимает готовый
 *      список игроков; метод printRoster() выводит состав.
 *   3. В main создайте несколько игроков ОТДЕЛЬНО, затем соберите из
 *      них команду и выведите состав.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Команда "Соколы":
 *   - Иван
 *   - Пётр
 *   - Анна
 *
 * ПОДСКАЗКА:
 *   В агрегации игроки существуют независимо от команды — их создают
 *   до неё и передают в конструктор.
 */

import java.util.ArrayList;
import java.util.List;

public class Task05 {
    public static void main(String[] args) {
        // Создайте игроков, затем команду из них, выведите состав
    }
}
