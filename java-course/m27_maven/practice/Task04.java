package m27_maven.practice;

/**
 * Задача 04 — Модуль 27: Жизненный цикл сборки (упражнение в терминале)
 *
 * ЗАДАНИЕ:
 *   Изучите фазы жизненного цикла на практике. Выполните по очереди
 *   и понаблюдайте, что делает каждая команда:
 *     1) mvn clean      — удалит папку target/
 *     2) mvn compile    — скомпилирует код в target/classes
 *     3) mvn test       — выполнит тесты (если есть)
 *     4) mvn package    — соберёт .jar в target/
 *   Запомните: запуск фазы выполняет все предыдущие фазы.
 *
 * ПРОВЕРКА:
 *   - после compile в target/classes появились .class файлы;
 *   - после package в target/ появился my-app-1.0.0.jar.
 *
 * ВОПРОС НА ПОНИМАНИЕ (ответьте в комментарии):
 *   Что выполнит команда `mvn package` помимо самой упаковки и почему?
 */
public class Task04 {
    public static void main(String[] args) {
        System.out.println("Выполните задание в терминале (см. комментарий выше).");
    }
}
