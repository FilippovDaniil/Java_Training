package m55_http_basics.practice;

/**
 * Задача 02 — Модуль 55: Выбор правильного HTTP статус-кода
 *
 * ЗАДАНИЕ:
 *   Для каждого сценария работы API «Заметки» выберите правильный
 *   HTTP статус-код и запишите в поле TODO.
 *   Также укажите, нужно ли возвращать тело ответа (да/нет).
 *
 *   Сценарии:
 *     1. Заметка успешно создана, в ответе — объект заметки с новым ID
 *     2. Заметка не найдена по переданному ID
 *     3. Запрос пришёл без Authorization-заголовка (токен отсутствует)
 *     4. Токен есть, но у пользователя нет прав на чужую заметку
 *     5. Заметка успешно удалена, тело ответа не нужно
 *     6. Поле title пустое — нарушение валидации
 *     7. Список заметок успешно возвращён
 *     8. Клиент запрашивает PUT /notes/42, но для этого пути PUT не разрешён
 *     9. Внутренняя ошибка: база данных недоступна
 *    10. Клиент прислал тело в формате XML, а сервер принимает только JSON
 *
 * ПОДСКАЗКА:
 *   Коды для ориентира: 200, 201, 204, 400, 401, 403, 404, 405, 415, 500
 *   Помните: 401 — «не знаю кто ты», 403 — «знаю, но не пущу».
 *   При 201 по RFC рекомендуется включить заголовок Location.
 */
public class Task02 {
    public static void main(String[] args) {
        System.out.println("=== Задача 02: HTTP статус-коды ===");
        System.out.println();

        String[] scenarios = {
            "1. Заметка успешно создана (в ответе — объект заметки с ID)",
            "2. Заметка не найдена по переданному ID",
            "3. Запрос без Authorization-заголовка (токен отсутствует)",
            "4. Токен есть, но нет прав на чужую заметку",
            "5. Заметка успешно удалена, тела нет",
            "6. Поле title пустое — ошибка валидации",
            "7. Список заметок успешно возвращён",
            "8. PUT /notes/42 не разрешён для этого пути",
            "9. База данных недоступна — внутренняя ошибка",
           "10. Клиент прислал XML, сервер принимает только JSON"
        };

        // TODO: заполните массив ответами в формате "КОД — Есть тело? (да/нет)"
        // Пример ожидаемого заполнения:
        //   "200 — да"
        //   "204 — нет"
        String[] answers = {
            "TODO — 201 - успешное создание ресурса",  // 1
            "TODO — 404 - невозможно найти ресурс",  // 2
            "TODO — 401 - не пройдена аутентификация",  // 3
            "TODO — 403 - не пройдена авторизация - нет прав на чтение",  // 4
            "TODO — 204 - ресурс успешно удалён",  // 5
            "TODO — 400 - ошибка валидации",  // 6
            "TODO — 200 - ресурс успешно возвращён", // 7
            "TODO — 405 - метод не разрешён для этого пути",  // 8
            "TODO — 500 - внутренняя ошибка сервиса",  // 9
            "TODO — 415 - не поддерживаемый формат тела"   // 10
        };

        for (int i = 0; i < scenarios.length; i++) {
            System.out.println("Сценарий: " + scenarios[i]);
            System.out.println("  Ваш ответ: " + answers[i]);
            System.out.println();
        }

        System.out.println("Бонус-вопрос: в каком сценарии рекомендуется добавить");
        System.out.println("заголовок Location в ответ? Что в нём должно быть?");
        System.out.println("В location передаётся url созданного ресурса. удобно когда сразу к нему надо обращаться");
    }
}
