/**
 * Задача 03 — Модуль 11: Инкапсуляция с валидацией
 *
 * ЗАДАНИЕ:
 *   У класса Person поле age сделайте private и предоставьте к нему
 *   доступ через геттер и сеттер. В сеттере проверяйте: возраст
 *   должен быть в диапазоне 0..120, иначе — вывести сообщение об
 *   ошибке и НЕ менять значение.
 *
 * ПРИМЕР:
 *   setAge(30)  → ок, getAge() вернёт 30
 *   setAge(-5)  → "Недопустимый возраст: -5", значение не меняется
 *   setAge(200) → "Недопустимый возраст: 200"
 *
 * ПОДСКАЗКА:
 *   private int age; public int getAge() {...} public void setAge(int age){...}
 */
public class Task03 {
    public static void main(String[] args) {
        // Создайте Person, попробуйте задать корректный и некорректный возраст
    }
}

class Person {
    private String name;
    private int age;

    // TODO: геттеры/сеттеры; в setAge — валидация 0..120
}
