/**
 * Задача 07 — Модуль 11 (МИНИ-ПРОЕКТ): Банковский счёт
 *
 * ЗАДАНИЕ:
 *   Реализуйте класс BankAccount с полной инкапсуляцией:
 *     - private поля: owner (String), balance (double);
 *     - конструктор (owner, начальный баланс >= 0);
 *     - геттеры getOwner(), getBalance();
 *     - метод deposit(double amount): пополнение;
 *       сумма должна быть > 0, иначе сообщение об ошибке;
 *     - метод withdraw(double amount): снятие;
 *       нельзя снять больше баланса или сумму <= 0 —
 *       выводите "Недостаточно средств" / "Некорректная сумма";
 *     - метод toString() для вывода счёта.
 *   В main создайте счёт и продемонстрируйте: успешное пополнение,
 *   успешное и неуспешное снятие.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Счёт Иван: 1000.0
 *   Пополнение на 500.0
 *   Счёт Иван: 1500.0
 *   Снятие 2000.0: Недостаточно средств
 *   Снятие 300.0
 *   Счёт Иван: 1200.0
 *
 * ПОДСКАЗКА:
 *   Баланс нельзя менять напрямую — только через deposit/withdraw.
 *   В этом и смысл инкапсуляции: класс сам следит за корректностью.
 */
public class Task07 {
    public static void main(String[] args) {
        // Создайте BankAccount и продемонстрируйте операции
    }
}

class BankAccount {
    // TODO: private поля, конструктор, геттеры, deposit, withdraw, toString
}
