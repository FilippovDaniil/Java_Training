/**
 * Задача 01 — Модуль 12: Статический счётчик объектов
 *
 * ЗАДАНИЕ:
 *   У класса User сделайте статическое поле count, которое
 *   увеличивается в конструкторе при каждом создании объекта.
 *   В main создайте несколько пользователей и выведите их общее
 *   количество через User.count.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (для 3 объектов):
 *   Создано пользователей: 3
 *
 * ПОДСКАЗКА:
 *   static int count = 0; в конструкторе count++.
 *   Обращение: User.count (через имя класса).
 */

public class Task01 {
    public static void main(String[] args) {
        // Создайте несколько User и выведите User.count
    }
}
