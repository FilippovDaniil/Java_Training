/**
 * Задача 03 — Модуль 22: Полиморфизм через массив интерфейса
 *
 * ЗАДАНИЕ:
 *   Дан интерфейс Animal с методом sound(). Реализуйте Dog, Cat, Cow.
 *   Создайте массив Animal[] из разных животных и в ОДНОМ цикле
 *   вызовите sound() для каждого — продемонстрируйте полиморфизм
 *   (один вызов — разное поведение).
 *
 * ОЖИДАЕМЫЙ ВЫВОД:
 *   Гав
 *   Мяу
 *   Му
 *
 * ПОДСКАЗКА:
 *   Animal[] animals = { new Dog(), new Cat(), new Cow() };
 *   for (Animal a : animals) a.sound();  // вызовется метод реального класса
 */

// TODO: классы Dog, Cat, Cow, реализующие Animal
public class Task03 {
    public static void main(String[] args) {
        // Создайте Animal[] и в цикле вызовите sound()
    }
}
