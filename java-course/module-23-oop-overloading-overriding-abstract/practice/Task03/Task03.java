/**
 * Задача 03 — Модуль 23: Абстрактный класс
 *
 * ЗАДАНИЕ:
 *   Объявлен абстрактный класс Shape с абстрактным методом area()
 *   и обычным методом printArea(). Реализуйте потомка Square
 *   (поле side), который определяет area().
 *   В main создайте квадрат и вызовите printArea().
 *   Попробуйте создать объект Shape напрямую (new Shape()) и убедитесь,
 *   что это НЕ компилируется (закомментируйте после проверки).
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример для side=4):
 *   Площадь: 16.0
 *
 * ПОДСКАЗКА:
 *   abstract class Shape { abstract double area(); ... }
 *   Потомок ОБЯЗАН реализовать area().
 */

// TODO: класс Square extends Shape с реализацией area()
public class Task03 {
    public static void main(String[] args) {
        // Создайте Square и вызовите printArea()
    }
}
