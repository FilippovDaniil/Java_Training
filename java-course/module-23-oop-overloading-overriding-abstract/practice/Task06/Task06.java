/**
 * Задача 06 — Модуль 23: toString и переопределение методов Object
 *
 * ЗАДАНИЕ:
 *   Дан класс Vector2D (x, y).
 *   1. Переопределите toString() для читаемого вывода "(x, y)".
 *   2. ПЕРЕГРУЗИТЕ метод add: add(Vector2D other) и add(int dx, int dy)
 *      — оба возвращают новый Vector2D.
 *   3. В main продемонстрируйте обе версии add и вывод через toString.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   v1 = (1, 2)
 *   v2 = (3, 4)
 *   v1 + v2 = (4, 6)
 *   v1 + (10,10) = (11, 12)
 *
 * ПОДСКАЗКА:
 *   Здесь сочетаются переопределение (toString) и перегрузка (add).
 */

public class Task06 {
    public static void main(String[] args) {
        // Создайте векторы, сложите их обеими версиями add
    }
}
