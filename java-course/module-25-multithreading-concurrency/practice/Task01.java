/**
 * Задача 01 — Модуль 25: Первый поток (Thread)
 *
 * ЗАДАНИЕ:
 *   Создайте класс-поток (наследник Thread), который в методе run()
 *   выводит числа от 1 до 5 с пометкой имени потока. В main создайте
 *   и ЗАПУСТИТЕ его методом start().
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Thread-0: 1
 *   Thread-0: 2
 *   ...
 *
 * ВНИМАНИЕ:
 *   Запускайте поток через start(), а НЕ run() (иначе нет нового потока).
 *
 * ПОДСКАЗКА:
 *   class CountThread extends Thread { public void run() {...} }
 *   getName() вернёт имя потока.
 */
public class Task01 {
    public static void main(String[] args) {
        // Создайте и запустите поток через start()
    }
}

// TODO: класс CountThread extends Thread
