/**
 * Задача 05 — Модуль 32: Профили (упражнение в терминале)
 *
 * ЗАДАНИЕ:
 *   Настройте два профиля окружения: dev и prod.
 *     1) в pom.xml добавьте секцию <profiles> с профилями dev (активен
 *        по умолчанию) и prod;
 *     2) в каждом профиле задайте свойство db.url с разным значением;
 *     3) выведите активное значение (например, через
 *        maven-antrun-plugin или просто проверьте в логах сборки,
 *        либо подставьте свойство в ресурс через filtering).
 *
 * ОЖИДАЕМЫЕ КОМАНДЫ:
 *   mvn help:active-profiles           # покажет активные профили
 *   mvn package                        # профиль dev
 *   mvn package -P prod                # профиль prod
 *
 * ПРОВЕРКА:
 *   help:active-profiles при обычном запуске показывает dev,
 *   а с флагом -P prod — prod.
 */
public class Task05 {
    public static void main(String[] args) {
        System.out.println("Выполните задание в терминале (см. комментарий выше).");
    }
}
