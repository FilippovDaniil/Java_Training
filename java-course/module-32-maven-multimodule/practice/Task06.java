/**
 * Задача 06 — Модуль 32: Сборка исполняемого jar (упражнение)
 *
 * ЗАДАНИЕ:
 *   Соберите из модуля app исполняемый «толстый» jar со всеми
 *   зависимостями (включая код common и сторонние библиотеки).
 *     1) подключите maven-shade-plugin (или assembly) в pom.xml app;
 *     2) укажите главный класс (Main-Class) в конфигурации;
 *     3) соберите и запустите получившийся jar.
 *
 * ОЖИДАЕМЫЕ КОМАНДЫ:
 *   mvn clean package
 *   java -jar app/target/app-1.0.0.jar
 *
 * ПРОВЕРКА:
 *   java -jar запускает приложение; оно использует классы из common
 *   и сторонних зависимостей — значит, они вошли в «толстый» jar.
 *
 * ПОДСКАЗКА:
 *   maven-shade-plugin при фазе package объединяет все классы и
 *   зависимости в один исполняемый артефакт.
 */
public class Task06 {
    public static void main(String[] args) {
        System.out.println("Выполните задание в терминале (см. комментарий выше).");
    }
}
