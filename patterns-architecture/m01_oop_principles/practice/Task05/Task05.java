package m01_oop_principles.practice.task05;

/**
 * Задача 05 — Тема 01: Композиция вместо наследования
 *
 * ЗАДАНИЕ:
 *   Нужен список строк, который логирует каждое добавление.
 *   НАИВНОЕ решение — extends ArrayList<String> и переопределить add().
 *   Оно ХРУПКОЕ: addAll() внутри вызывает add() по-разному в разных версиях
 *   JDK, и счётчик/лог «врёт». Сделайте через КОМПОЗИЦИЮ:
 *     - класс CountingList (файл CountingList.java) ХРАНИТ внутри List<String>
 *       (не наследует его), а наружу даёт только нужные методы:
 *         add(String), addAll(Collection<String>), size(), addedCount();
 *     - каждое добавление увеличивает счётчик addedCount.
 *   В main добавьте несколько элементов поштучно и пачкой, выведите счётчик.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Размер: 4
 *   Всего добавлений зафиксировано: 4
 *
 * ТРЕБОВАНИЯ:
 *   - CountingList НЕ наследует ArrayList/List (никакого extends);
 *   - внутри — приватное поле List<String> (делегирование);
 *   - addAll увеличивает счётчик на число реально добавленных элементов
 *     (не должен «терять» элементы из-за того, как устроен ArrayList внутри).
 *
 * ПОДСКАЗКА:
 *   «Favor composition over inheritance»: оберни List и делегируй вызовы.
 *   Так ты контролируешь счётчик сам, а не зависишь от внутренней реализации
 *   родителя.
 */

public class Task05 {
    public static void main(String[] args) {
        // TODO: создайте CountingList, добавьте элементы поштучно и через addAll,
        //       выведите size() и addedCount()
    }
}
