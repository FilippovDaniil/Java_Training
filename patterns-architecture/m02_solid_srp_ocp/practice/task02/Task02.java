package m02_solid_srp_ocp.practice.task02;

/**
 * Задача 02 — Тема 02: SRP — вынести «чужую» ответственность
 *
 * ЗАДАНИЕ:
 *   Класс RegistrationService раньше делал три вещи: проверял корректность
 *   e-mail, формировал приветственное письмо и «регистрировал» пользователя.
 *   Проверка формата e-mail — отдельная ответственность (правила валидации
 *   меняются независимо от регистрации). Вынесите её:
 *     - EmailValidator (файл EmailValidator.java): boolean isValid(String email)
 *       (простое правило: непусто, содержит '@' и '.');
 *     - RegistrationService использует EmailValidator, а сам только
 *       регистрирует (хранит зарегистрированные e-mail) и возвращает
 *       приветствие.
 *   В main зарегистрируйте корректный и некорректный e-mail.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Регистрация ok@mail.com: Добро пожаловать, ok@mail.com
 *   Регистрация bad-email: отклонено (неверный e-mail)
 *   Зарегистрировано всего: 1
 *
 * ТРЕБОВАНИЯ:
 *   - правило валидации живёт ТОЛЬКО в EmailValidator;
 *   - RegistrationService не содержит проверок формата, а вызывает валидатор;
 *   - две причины для изменения (правила e-mail / логика регистрации) разведены.
 *
 * ПОДСКАЗКА:
 *   RegistrationService получает EmailValidator (можно создать внутри или
 *   передать в конструктор — про передачу зависимостей подробно в теме 03/16).
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: создайте RegistrationService, зарегистрируйте корректный и
        //       некорректный e-mail, выведите итоговое число зарегистрированных
    }
}
