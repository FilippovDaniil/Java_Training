package m03_solid_lsp_isp_dip.practice.task04;

/**
 * Задача 04 — Тема 03: ISP — ролевые интерфейсы устройства
 *
 * ЗАДАНИЕ:
 *   Вместо одного «толстого» интерфейса офисной техники сделайте три ролевых:
 *     - Printer (файл Printer.java): void print(String doc);
 *     - Scanner (файл Scanner.java): void scan(String doc);
 *     - Fax     (файл Fax.java):     void fax(String doc);
 *   Реализации:
 *     - SimplePrinter — только Printer;
 *     - Mfu (МФУ) — Printer + Scanner + Fax.
 *   В main распечатайте документ на обоих, отсканируйте и отправьте факс
 *   только на том, кто это умеет.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Печать: договор.pdf
 *   Печать: договор.pdf
 *   Сканирование: фото.jpg
 *   Факс: отчёт.txt
 *
 * ТРЕБОВАНИЯ:
 *   - SimplePrinter не обязан реализовывать scan/fax (никаких заглушек);
 *   - клиент, которому нужна печать, зависит только от Printer;
 *   - комбинация ролей в Mfu — через реализацию нескольких интерфейсов.
 *
 * ПОДСКАЗКА:
 *   Узкие интерфейсы можно свободно комбинировать (Mfu реализует все три),
 *   но никто не платит за то, что ему не нужно (SimplePrinter — только печать).
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: создайте SimplePrinter и Mfu; печать на обоих;
        //       скан и факс — только на Mfu
    }
}
