package m04_singleton_factory_method.practice.task02;

/**
 * Задача 02 — Тема 04: Singleton через enum
 *
 * ЗАДАНИЕ:
 *   Реализуйте конфигурацию приложения как enum-Singleton (рекомендуемый
 *   способ в Java):
 *     - enum AppConfig (файл AppConfig.java) с единственным INSTANCE;
 *     - приватные поля настроек, например timeoutSeconds (по умолч. 30) и
 *       appName (по умолч. "OPS");
 *     - геттеры и метод setTimeoutSeconds(int) с проверкой (> 0).
 *   В main прочитайте настройки через AppConfig.INSTANCE, измените таймаут,
 *   убедитесь, что изменение видно при повторном чтении (состояние общее).
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Приложение: OPS, таймаут: 30
 *   После изменения таймаут: 60
 *
 * ТРЕБОВАНИЯ:
 *   - именно enum с одним элементом INSTANCE (не class с конструктором);
 *   - состояние общее: изменив таймаут, при следующем доступе видим новое;
 *   - setTimeoutSeconds отвергает не положительные значения.
 *
 * ПОДСКАЗКА:
 *   enum-Singleton бесплатно даёт потокобезопасность и защиту от рефлексии/
 *   сериализации. Поля enum инициализируются в его (неявном) конструкторе.
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: прочитайте AppConfig.INSTANCE, измените таймаут, прочитайте снова
    }
}
