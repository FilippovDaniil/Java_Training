package m06_prototype_pool.practice.task03;

/**
 * Задача 03 — Тема 06: поверхностная vs глубокая копия
 *
 * ЗАДАНИЕ:
 *   Реализуйте Team (файл Team.java) с изменяемым вложенным списком и ДВУМЯ
 *   способами копирования, чтобы наглядно увидеть разницу:
 *     - поле name (String) и members (List<String>);
 *     - Team shallowCopy(): новый Team, но СПИСОК участников тот же (по ссылке);
 *     - Team deepCopy():   новый Team и НОВЫЙ список участников;
 *     - метод addMember(String).
 *   В main: создайте команду, сделайте обе копии, добавьте участника КАЖДОЙ
 *   копии и покажите, как меняется (или не меняется) оригинал.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   После add в shallow-копию оригинал: [Аня, Боря]   <- протёк! (общий список)
 *   После add в deep-копию оригинал:    [Аня, Боря]   <- не изменился (свой список)
 *
 * ТРЕБОВАНИЯ:
 *   - shallowCopy делит список с оригиналом (демонстрация бага);
 *   - deepCopy полностью независим (new ArrayList<>(members));
 *   - в выводе видно разницу между двумя видами копии.
 *
 * ПОДСКАЗКА:
 *   Глубокая копия нужна именно для ИЗМЕНЯЕМЫХ вложенных объектов. Если бы
 *   список был неизменяемым (List.of/Collections.unmodifiable), делить ссылку
 *   было бы безопасно.
 */

public class Task03 {
    public static void main(String[] args) {
        // TODO: создайте Team, снимите shallowCopy и deepCopy, поменяйте каждую,
        //       сравните влияние на оригинал
    }
}
