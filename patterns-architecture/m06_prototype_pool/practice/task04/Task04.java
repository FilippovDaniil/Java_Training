package m06_prototype_pool.practice.task04;

/**
 * Задача 04 — Тема 06: Prototype-реестр (штамповка по образцу)
 *
 * ЗАДАНИЕ:
 *   Сделайте реестр образцов фигур и создавайте новые фигуры их клонированием:
 *     - интерфейс Shape (файл Shape.java): Shape copy(); String describe();
 *     - Circle (radius) и Rectangle (w, h) реализуют copy() (возвращают свою
 *       независимую копию) и describe();
 *     - ShapeRegistry (файл ShapeRegistry.java): register(String key, Shape proto)
 *       и Shape create(String key) — возвращает КОПИЮ образца (proto.copy()),
 *       а не сам образец.
 *   В main зарегистрируйте образцы "circle" и "rect", создайте по копии каждого,
 *   убедитесь, что create возвращает НОВЫЙ объект (не тот же, что образец).
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   circle -> Circle(r=5)
 *   rect -> Rectangle(3x4)
 *   Это копия, не образец: true
 *
 * ТРЕБОВАНИЯ:
 *   - create(key) возвращает proto.copy(), не сам прототип (иначе все «копии»
 *     были бы одним объектом);
 *   - добавление нового вида фигуры = register нового образца, без правок реестра;
 *   - проверьте через != , что create вернул новый объект.
 *
 * ПОДСКАЗКА:
 *   Реестр прототипов удобен для типовых заготовок: один раз настроил образец —
 *   дальше «штампуешь» его копии по ключу.
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: зарегистрируйте образцы в ShapeRegistry, создайте копии через create,
        //       проверьте, что это новые объекты
    }
}
