package m08_proxy_facade.practice.task04;

/**
 * Задача 04 — Тема 08: Facade (упрощённый вход в подсистему)
 *
 * ЗАДАНИЕ:
 *   Спрячьте запуск компьютера из трёх подсистем за одним фасадом:
 *     - Cpu (файл Cpu.java): String boot() → "CPU запущен";
 *     - Memory (файл Memory.java): String load() → "память загружена";
 *     - Disk (файл Disk.java): String read() → "диск прочитан";
 *     - Computer (файл Computer.java) — ФАСАД: внутри держит Cpu, Memory, Disk;
 *       метод start() вызывает их в нужном порядке и печатает каждый шаг.
 *   В main создайте Computer и вызовите start() — клиент не знает о подсистемах.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   CPU запущен
 *   память загружена
 *   диск прочитан
 *
 * ТРЕБОВАНИЯ:
 *   - клиент (main) обращается ТОЛЬКО к Computer.start();
 *   - фасад знает порядок вызовов подсистем и координирует их;
 *   - фасад только координирует, не реализует логику подсистем сам.
 *
 * ПОДСКАЗКА:
 *   Facade не запрещает доступ к Cpu/Memory/Disk напрямую — он лишь даёт удобный
 *   «вход» для типового сценария запуска.
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: создайте Computer и вызовите start()
    }
}
