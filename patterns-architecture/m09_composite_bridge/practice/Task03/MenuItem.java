package m09_composite_bridge.practice.task03;

class MenuItem implements MenuComponent {
    // TODO: поле name + конструктор MenuItem(String name)

    @Override
    public String render(int indent) {
        // TODO: вернуть "  ".repeat(indent) + name + "\n"
        return "";
    }
}
