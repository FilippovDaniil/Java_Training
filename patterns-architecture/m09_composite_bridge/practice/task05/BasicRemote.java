package m09_composite_bridge.practice.task05;

// Простой пульт — только базовое поведение RemoteControl.
class BasicRemote extends RemoteControl {
    public BasicRemote(Device device) {
        super(device);
    }
}
