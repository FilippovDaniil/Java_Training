package m09_composite_bridge.practice.task07;

class CsvFormat implements StatementFormat {
    @Override
    public String format(AccountComponent node) {
        // TODO: node.name() + ";" + node.totalBalanceCents()
        return "";
    }
}
