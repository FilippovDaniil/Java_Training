package m11_observer_chain_of_responsibility.practice.task03;

class HighHandler extends Handler {
    @Override
    public String handle(int n) {
        // TODO: "high: " + n (последний в цепочке обрабатывает остальное)
        return "";
    }
}
