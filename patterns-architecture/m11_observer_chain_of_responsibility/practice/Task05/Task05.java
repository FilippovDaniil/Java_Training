package m11_observer_chain_of_responsibility.practice.task05;

/**
 * Задача 05 — Тема 11: Chain of Responsibility как конвейер валидации
 *
 * ЗАДАНИЕ:
 *   Цепочка валидаторов проверяет заявку: каждый либо пропускает дальше, либо
 *   останавливает цепочку с ошибкой (здесь обработать должны ВСЕ по очереди):
 *     - Request (файл Request.java): данные name (String), age (int);
 *     - абстрактный Validator (файл Validator.java): next, setNext, abstract
 *       String validate(Request r), protected passToNext(Request r) — если next
 *       есть, делегировать, иначе вернуть "ok";
 *     - NameValidator (имя непустое, иначе "имя пустое") и AgeValidator
 *       (age >= 18, иначе "возраст < 18").
 *   В main прогоните корректную заявку и заявку с возрастом 15.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Аня/20: ok
 *   Аня/15: возраст < 18
 *
 * ТРЕБОВАНИЯ:
 *   - валидатор при успехе передаёт дальше, при ошибке — прерывает цепочку;
 *   - если все прошли — результат "ok";
 *   - новый валидатор добавляется в цепочку без правок остальных.
 *
 * ПОДСКАЗКА:
 *   Отличие от Task03/04: там обработчик ищется ОДИН; здесь по цепочке должны
 *   пройти ВСЕ проверки, а первая неудача останавливает конвейер.
 */

public class Task05 {
    public static void main(String[] args) {
        // TODO: соберите цепочку NameValidator → AgeValidator, проверьте две заявки
    }
}
