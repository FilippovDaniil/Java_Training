package m12_state_template_method.practice.task03;

class UpperPipeline extends TextPipeline {
    @Override
    protected String transform(String input) {
        // TODO: input.toUpperCase()
        return "";
    }
    // addExclaim не переопределяем — остаётся false
}
