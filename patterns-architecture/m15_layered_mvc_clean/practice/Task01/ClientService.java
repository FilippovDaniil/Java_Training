package m15_layered_mvc_clean.practice.task01;

// Business: бизнес-логика; зависит от слоя Persistence (репозитория).
class ClientService {
    // TODO: поле ClientRepository repo + конструктор ClientService(ClientRepository repo)

    public String describe(String id) {
        // TODO: найти клиента; если есть → "Клиент: " + name; иначе "не найден"
        return "";
    }
}
