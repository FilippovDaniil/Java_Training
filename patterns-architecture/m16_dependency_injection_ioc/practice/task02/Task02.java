package m16_dependency_injection_ioc.practice.task02;

/**
 * Задача 02 — Тема 16: DI позволяет подменять реализацию
 *
 * ЗАДАНИЕ:
 *   Покажите главную выгоду DI: один и тот же потребитель работает с разными
 *   реализациями, выбираемыми снаружи.
 *     - интерфейс MessageService (файл MessageService.java): String message(String name);
 *     - FormalMessageService ("Уважаемый, X") и CasualMessageService ("Привет, X");
 *     - Greeter (файл Greeter.java): MessageService через конструктор; greet(name).
 *   В main создайте ДВА Greeter — с формальным и с неформальным сервисом — и
 *   поздоровайтесь каждым. Код Greeter при этом один и тот же.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Уважаемый, Аня
 *   Привет, Аня
 *
 * ТРЕБОВАНИЯ:
 *   - Greeter не меняется при смене реализации сервиса;
 *   - конкретная реализация выбирается в точке сборки (main), не внутри Greeter;
 *   - обе реализации взаимозаменяемы (зависимость на интерфейс).
 *
 * ПОДСКАЗКА:
 *   Возможность подставить нужную реализацию (в т.ч. тестового двойника) — прямое
 *   следствие DI. В Spring это делает контейнер по конфигурации.
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: создайте Greeter с Formal и с Casual сервисом, поздоровайтесь каждым
    }
}
