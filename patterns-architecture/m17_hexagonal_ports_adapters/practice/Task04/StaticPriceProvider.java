package m17_hexagonal_ports_adapters.practice.task04;

class StaticPriceProvider implements PriceProvider {
    @Override
    public long priceOf(String sku) {
        // TODO: sku.length() * 100L
        return 0;
    }
}
