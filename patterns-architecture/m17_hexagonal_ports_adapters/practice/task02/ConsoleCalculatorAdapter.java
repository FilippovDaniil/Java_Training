package m17_hexagonal_ports_adapters.practice.task02;

// Driving-адаптер: вызывает ядро через входной порт (в проде — REST-контроллер).
class ConsoleCalculatorAdapter {
    // TODO: поле final CalculatorUseCase useCase + конструктор

    public void run(long a, long b) {
        // TODO: напечатать "результат: " + useCase.add(a, b)
    }
}
