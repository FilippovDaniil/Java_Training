package m19_ddd_tactical.practice.task04;

// Фабрика: собирает Account и гарантирует валидное начальное состояние.
class AccountFactory {
    private int counter = 0;

    public Account openNew(String ownerId, long initialDepositCents) {
        // TODO: id = "ACC-" + (++counter); создать Account(id, ownerId);
        //       если initialDepositCents > 0 → account.deposit(initialDepositCents);
        //       вернуть account
        return null;
    }
}
