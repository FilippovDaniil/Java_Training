package m21_saga_distributed_transactions.practice.task04;

class ShippingParticipant {
    public void register(EventBus bus) {
        // TODO: bus.subscribe(PaymentDone.class, e -> напечатать "отгрузка " + e.orderId());
    }
}
