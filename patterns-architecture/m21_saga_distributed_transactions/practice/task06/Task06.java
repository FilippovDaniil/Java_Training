package m21_saga_distributed_transactions.practice.task06;

/**
 * Задача 06 — Тема 21: идемпотентный шаг саги (защита от повторов)
 *
 * ЗАДАНИЕ:
 *   В распределённой системе сообщения доставляются ПОВТОРНО — и при оркестрации,
 *   и при хореографии. Поэтому шаги должны быть идемпотентны: повторный вызов не
 *   должен дублировать эффект.
 *     - IdempotentReserve (файл IdempotentReserve.java): хранит Set<String>
 *       обработанных orderId; reserve(orderId):
 *         если id уже обработан → печатает "уже зарезервировано: <id>" (ничего не делает);
 *         иначе → печатает "резерв: <id>" и запоминает id.
 *   В main: зарезервируйте "A" (резерв), повторно "A" (уже зарезервировано),
 *   затем "B" (резерв).
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   резерв: A
 *   уже зарезервировано: A
 *   резерв: B
 *
 * ТРЕБОВАНИЯ:
 *   - повторный reserve того же id НЕ создаёт второй резерв (идемпотентность);
 *   - факт обработки запоминается (Set обработанных id);
 *   - новый id обрабатывается как обычно.
 *
 * ПОДСКАЗКА:
 *   Идемпотентность через дедупликацию по id: if (!seen.add(id)) return; ... .
 *   Это страхует сагу от двойного списания/резерва при ретраях сообщений.
 */

public class Task06 {
    public static void main(String[] args) {
        // TODO: IdempotentReserve; reserve("A"), reserve("A"), reserve("B")
    }
}
