package m22_antipatterns.practice.task03;

/**
 * Задача 03 — Тема 22: лечим Primitive Obsession через Value Object
 *
 * ЗАДАНИЕ:
 *   Раньше деньги представляли «голым» long — терялись валюта и валидация, легко
 *   перепутать аргументы. Введите Value Object Money (файл Money.java):
 *     - неизменяемые amountCents (long), currency (String); конструктор с проверкой
 *       (amount >= 0, currency не пустой);
 *     - plus(Money) — новый Money (валюты совпадают);
 *     - equals/hashCode по значению; toString "X RUB".
 *   В main замените «голые» long на Money: создайте две суммы, сложите, выведите;
 *   покажите, что отрицательная сумма отвергается.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   100 RUB + 50 RUB = 150 RUB
 *   Ошибка: сумма не может быть отрицательной
 *
 * ТРЕБОВАНИЯ:
 *   - тип Money несёт смысл и валидацию (не «голый» long);
 *   - операции возвращают новый Money (неизменяемость);
 *   - отрицательная сумма / разные валюты отвергаются.
 *
 * ПОДСКАЗКА:
 *   Primitive Obsession лечится Value Object (тема 18). Типизированные деньги
 *   нельзя случайно сложить с «количеством» или перепутать с id.
 */

public class Task03 {
    public static void main(String[] args) {
        // TODO: создайте два Money, сложите, выведите; попробуйте отрицательную сумму
    }
}
