package m23_microservices_vs_monolith.practice.task01;

/**
 * Задача 01 — Тема 23: модуль с публичным API и скрытыми внутренностями
 *
 * ЗАДАНИЕ:
 *   Оформите «модуль Счета» с узким публичным контрактом, спрятав реализацию:
 *     - AccountModule (файл AccountModule.java) — публичный API: long balance(String accountId);
 *     - AccountModuleImpl (файл AccountModuleImpl.java) — реализация со СКРЫТЫМ
 *       внутренним хранилищем (Map), плюс вспомогательный метод seed(id, balance)
 *       для наполнения; balance(id) возвращает баланс.
 *   В main работайте с модулем ЧЕРЕЗ интерфейс AccountModule.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Баланс A-1: 10000
 *
 * ТРЕБОВАНИЯ:
 *   - другие модули видят только AccountModule (контракт), не реализацию;
 *   - внутреннее хранилище приватно (инкапсуляция модуля);
 *   - main обращается к модулю через интерфейс.
 *
 * ПОДСКАЗКА:
 *   Это инкапсуляция (тема 01) в масштабе МОДУЛЯ. Чёткая граница модуля —
 *   заготовка для возможного выделения его в отдельный сервис.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: создайте AccountModuleImpl, наполните, используйте через AccountModule
    }
}
