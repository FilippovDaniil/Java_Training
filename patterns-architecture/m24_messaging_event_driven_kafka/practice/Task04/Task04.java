package m24_messaging_event_driven_kafka.practice.task04;

/**
 * Задача 04 — Тема 24: идемпотентный консьюмер (at-least-once)
 *
 * ЗАДАНИЕ:
 *   При гарантии at-least-once сообщения могут прийти повторно — консьюмер должен
 *   обработать каждое РОВНО раз, дедуплицируя по id:
 *     - IdempotentConsumer (файл IdempotentConsumer.java): хранит Set<String>
 *       обработанных id; onMessage(String id, String payload):
 *         если id уже обработан → ничего (можно напечатать "дубль пропущен: id");
 *         иначе → запомнить id и напечатать "обработано: payload";
 *       processedCount() — число уникальных обработанных.
 *   В main: доставьте сообщение id="m1" ДВАЖДЫ и id="m2" один раз; выведите
 *   число уникально обработанных.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   обработано: оплата
 *   дубль пропущен: m1
 *   обработано: доставка
 *   Обработано уникальных: 2
 *
 * ТРЕБОВАНИЯ:
 *   - повторная доставка того же id НЕ приводит к повторной обработке;
 *   - дедуп по id сообщения (Set обработанных);
 *   - уникально обработано ровно столько, сколько разных id.
 *
 * ПОДСКАЗКА:
 *   if (!processed.add(id)) { дубль; return; } handle(payload);
 *   Идемпотентность (тема 21) — стандартный спутник at-least-once.
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: onMessage("m1","оплата"); onMessage("m1","оплата"); onMessage("m2","доставка");
        //       вывести processedCount()
    }
}
