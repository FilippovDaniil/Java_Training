package m25_architecture_testing_archunit.practice.task01;

/**
 * Задача 01 — Тема 25: проверка направления зависимостей слоёв
 *
 * ЗАДАНИЕ:
 *   Представьте архитектуру графом зависимостей и проверьте правило слоёв:
 *     - LayerChecker (файл LayerChecker.java): метод
 *       List<String> check(Map<String, Set<String>> deps):
 *         правило — "domain" НЕ должен зависеть от "web" и "infrastructure";
 *         для каждой запрещённой зависимости domain добавляет в результат строку
 *         "domain зависит от <X>". Пустой список = правило соблюдено.
 *   В main: задайте граф, где domain ошибочно зависит от "web", прогоните проверку,
 *   выведите число нарушений и их список.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Нарушений: 1
 *   - domain зависит от web
 *
 * ТРЕБОВАНИЯ:
 *   - правило сформулировано как функция над графом зависимостей;
 *   - возвращается СПИСОК нарушений (пустой = тест пройден);
 *   - проверяется именно структура (кто от кого зависит), а не поведение.
 *
 * ПОДСКАЗКА:
 *   Это вручную делает то, что ArchUnit извлекает из байт-кода. deps.get("domain")
 *   — множество зависимостей домена; ищем в нём запрещённые.
 */

import java.util.Map;
import java.util.Set;

public class Task01 {
    public static void main(String[] args) {
        // TODO: соберите Map<String,Set<String>> deps (domain зависит от "web"),
        //       прогоните LayerChecker.check, выведите нарушения
    }
}
