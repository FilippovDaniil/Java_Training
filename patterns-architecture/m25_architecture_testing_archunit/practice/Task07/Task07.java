package m25_architecture_testing_archunit.practice.task07;

/**
 * Задача 07 — Тема 25 (МИНИ-ПРОЕКТ BAM): арх-тест гексагона
 *
 * Развитие BAM. Закрепим главное правило гексагона (тема 17) исполняемой
 * проверкой: ЯДРО не должно зависеть от адаптеров.
 *
 * ЗАДАНИЕ:
 *   - HexagonArchTest (файл HexagonArchTest.java): List<String> verify(
 *     Map<String,Set<String>> deps) — правило: "core" не должен зависеть от
 *     "web_adapter" и "db_adapter"; за каждую такую зависимость добавляет
 *     "core зависит от адаптера <X>".
 *   В main проверьте ДВА графа:
 *     1) корректный гексагон: core→{}, web_adapter→{core}, db_adapter→{core}
 *        → 0 нарушений (тест пройден);
 *     2) сломанный: core→{db_adapter} → 1 нарушение.
 *   Выведите число нарушений для каждого и список для сломанного.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Корректный гексагон — нарушений: 0
 *   Сломанный гексагон — нарушений: 1
 *   - core зависит от адаптера db_adapter
 *
 * ТРЕБОВАНИЯ:
 *   - правило проверяет именно «ядро → адаптеры» (Dependency Rule, тема 17);
 *   - корректный гексагон даёт 0 нарушений (зелёная сборка);
 *   - нарушение показывает, от какого адаптера зависит ядро.
 *
 * ПОДСКАЗКА:
 *   Это формализация темы 17 как теста: в реальном проекте ArchUnit проверял бы,
 *   что пакет core не зависит от пакетов *.adapter. Здесь — на графе зависимостей.
 */

import java.util.Map;
import java.util.Set;

public class Task07 {
    public static void main(String[] args) {
        // TODO: проверьте корректный и сломанный графы через HexagonArchTest.verify
    }
}
