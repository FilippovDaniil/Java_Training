package m25_architecture_testing_archunit.practice.task02;

/**
 * Задача 02 — Тема 25: проверка отсутствия циклов между модулями
 *
 * ЗАДАНИЕ:
 *   Циклические зависимости (A→B→A) — запах сильной связанности. Напишите детектор:
 *     - CycleDetector (файл CycleDetector.java): boolean hasCycle(Map<String,
 *       Set<String>> deps) — есть ли цикл в графе зависимостей (обход в глубину
 *       с отслеживанием вершин в текущем пути).
 *   В main: задайте граф с циклом (A→B, B→A) и проверьте; затем граф без цикла
 *   (A→B, B→C) и проверьте.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Цикл в [A->B, B->A]: true
 *   Цикл в [A->B, B->C]: false
 *
 * ТРЕБОВАНИЯ:
 *   - hasCycle корректно находит цикл (в т.ч. косвенный);
 *   - на ациклическом графе возвращает false;
 *   - проверка структурная (по графу зависимостей).
 *
 * ПОДСКАЗКА:
 *   Классический DFS: множество visited (полностью обработанные) + inStack
 *   (вершины текущего пути). Если дошли до вершины, уже лежащей в inStack — цикл.
 */

import java.util.Map;
import java.util.Set;

public class Task02 {
    public static void main(String[] args) {
        // TODO: проверьте hasCycle на графе с циклом и без него
    }
}
