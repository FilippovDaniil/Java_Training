package m26_legacy_refactoring_capstone.practice.task03;

// Гоняет оба варианта, логирует расхождения, возвращает результат legacy.
class ParallelRunner {
    // TODO: поля final Calculator legacy, final Calculator modern + конструктор

    public long run(long input) {
        // TODO: l = legacy.calc(input); m = modern.calc(input);
        //       если l != m → печать "расхождение на " + input + ": legacy=" + l + ", modern=" + m;
        //       вернуть l (источник истины — legacy)
        return 0;
    }
}
