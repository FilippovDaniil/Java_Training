package m26_legacy_refactoring_capstone.practice.task06;

/**
 * Задача 06 — Тема 26: шов (seam) для тестируемости legacy
 *
 * ЗАДАНИЕ:
 *   Старый код напрямую дёргал неуправляемую зависимость (например, Math.random) —
 *   его нельзя предсказуемо протестировать. Введите ШОВ (seam) — интерфейс,
 *   через который зависимость можно подменить:
 *     - NumberSource (файл NumberSource.java): int next();
 *     - Dice (файл Dice.java): получает NumberSource в конструкторе; roll()
 *       возвращает source.next() (раньше тут был прямой вызов случайности);
 *     - FixedNumberSource (файл FixedNumberSource.java): всегда возвращает
 *       заданное число (детерминированный двойник для теста).
 *   В main: создайте Dice с FixedNumberSource(4) и выведите roll() — результат
 *   предсказуем.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Бросок (через шов): 4
 *
 * ТРЕБОВАНИЯ:
 *   - неуправляемая зависимость спрятана за интерфейсом NumberSource (шов);
 *   - в тесте вместо случайности подставляется детерминированный источник;
 *   - Dice не вызывает случайность напрямую — берёт число из шва.
 *
 * ПОДСКАЗКА:
 *   «Seam» (Майкл Фезерс, «Working Effectively with Legacy Code») — место, где
 *   поведение можно подменить без правки кода вокруг. Это DI (тема 16) как способ
 *   сделать legacy тестируемым.
 */

public class Task06 {
    public static void main(String[] args) {
        // TODO: new Dice(new FixedNumberSource(4)); вывести roll()
    }
}
