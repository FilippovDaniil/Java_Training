/**
 * Задача 01 — Тема 01: Инкапсуляция
 *
 * ЗАДАНИЕ:
 *   Реализуйте класс Product (файл Product.java) с защищённым состоянием:
 *     - приватные поля: name (String), priceCents (long — цена в копейках);
 *     - конструктор с проверкой: name не пустой, priceCents >= 0;
 *     - геттеры getName(), getPriceCents();
 *     - метод applyDiscount(int percent): уменьшает цену на percent (0..100),
 *       при неверном проценте — IllegalArgumentException.
 *   Снаружи НЕЛЬЗЯ привести Product в некорректное состояние.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Молоко: 8000 коп.
 *   После скидки 10%: 7200 коп.
 *   Ошибка: процент скидки должен быть 0..100
 *
 * ТРЕБОВАНИЯ:
 *   - поля только private;
 *   - инварианты (имя не пустое, цена >= 0) проверяются в конструкторе и методах;
 *   - никаких публичных сеттеров, ломающих инварианты.
 *
 * ПОДСКАЗКА:
 *   Цену храните в копейках (long), чтобы избежать ошибок округления double.
 *   applyDiscount: priceCents = priceCents - priceCents * percent / 100.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: создайте Product, выведите цену, примените скидку,
        //       попробуйте передать неверный процент (поймайте исключение)
    }
}
