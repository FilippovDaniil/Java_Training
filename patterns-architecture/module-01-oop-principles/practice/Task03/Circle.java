class Circle implements Shape {

    // TODO: поле radius + конструктор Circle(double radius)

    @Override
    public double area() {
        // TODO: площадь круга = Math.PI * radius * radius
        return 0;
    }
}
