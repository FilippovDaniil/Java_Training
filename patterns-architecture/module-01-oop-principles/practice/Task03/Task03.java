/**
 * Задача 03 — Тема 01: Полиморфизм
 *
 * ЗАДАНИЕ:
 *   Реализуйте полиморфный расчёт площади:
 *     - интерфейс Shape (файл Shape.java) с методом double area();
 *     - Circle (радиус) и Rectangle (ширина, высота) реализуют Shape.
 *   В main сложите фигуры в List<Shape> и посчитайте суммарную площадь,
 *   перебирая список и вызывая area() — БЕЗ instanceof и проверок типа.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Площадь круга r=2: 12.57
 *   Площадь прямоугольника 3x4: 12.00
 *   Суммарная площадь: 24.57
 *
 * ТРЕБОВАНИЯ:
 *   - НИКАКИХ if (s instanceof ...) — выбор реализации делает полиморфизм;
 *   - каждая фигура сама знает, как считать свою площадь.
 *
 * ПОДСКАЗКА:
 *   Math.PI * r * r для круга. Цикл по List<Shape> вызывает s.area() — JVM
 *   сама выберет нужную реализацию (динамическая диспетчеризация).
 */

import java.util.List;

public class Task03 {
    public static void main(String[] args) {
        // TODO: создайте List<Shape> из Circle и Rectangle, просуммируйте area()
    }
}
