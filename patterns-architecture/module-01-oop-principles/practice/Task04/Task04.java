/**
 * Задача 04 — Тема 01: Абстракция (контракт через interface)
 *
 * ЗАДАНИЕ:
 *   Опишите абстракцию способа оплаты:
 *     - интерфейс PaymentMethod (файл PaymentMethod.java):
 *         String name();
 *         boolean pay(long amountCents);   // true — успех
 *     - CardPayment: хранит баланс карты; pay() списывает, если хватает средств;
 *     - CashPayment: всегда успешно (наличные «всегда есть»), печатает чек.
 *   В main создайте оба способа как PaymentMethod (тип — интерфейс!) и
 *   проведите по платежу через каждый.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Карта: оплата 5000 — успех (остаток 5000)
 *   Наличные: оплата 5000 — успех
 *   Карта: оплата 9999 — отказ (недостаточно средств)
 *
 * ТРЕБОВАНИЯ:
 *   - вызывающий код работает с типом PaymentMethod, не зная конкретного класса;
 *   - детали реализации (баланс карты, печать чека) скрыты за контрактом.
 *
 * ПОДСКАЗКА:
 *   Абстракция = «что делает» (интерфейс), а «как именно» — в реализациях.
 *   Это фундамент для DIP (тема 03) и почти всех паттернов.
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: создайте PaymentMethod card = new CardPayment(...) и cash = new CashPayment();
        //       проведите платежи, выведите результат
    }
}
