/**
 * Задача 06 — Тема 01: Инкапсуляция + полиморфизм вместе
 *
 * ЗАДАНИЕ:
 *   Соедините инкапсуляцию (защита баланса) и полиморфизм (разное поведение
 *   подтипов):
 *     - абстрактный класс Account (файл Account.java): приватный balanceCents,
 *       методы deposit(amount) и withdraw(amount) с инвариантами
 *       (сумма > 0; нельзя уйти в минус сверх допустимого), геттер баланса;
 *       абстрактный метод applyMonthly() — месячное начисление/списание;
 *     - SavingsAccount: applyMonthly() начисляет 5% на баланс;
 *     - CheckingAccount: applyMonthly() списывает фиксированную плату 100 коп.
 *   В main создайте List<Account>, проведите операции, вызовите applyMonthly()
 *   у каждого полиморфно и выведите итоговые балансы.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Накопительный: 10500 (после +5%)
 *   Расчётный: 9900 (после -100)
 *
 * ТРЕБОВАНИЯ:
 *   - balanceCents строго private, меняется только через deposit/withdraw/applyMonthly;
 *   - withdraw защищает инвариант (нельзя списать больше, чем разрешено);
 *   - applyMonthly() вызывается полиморфно через тип Account.
 *
 * ПОДСКАЗКА:
 *   Это «репетиция» проекта BAM (банковские счета) из темы 03 и далее.
 *   Инкапсуляция стережёт деньги, полиморфизм даёт разное поведение типов счёта.
 */

import java.util.List;

public class Task06 {
    public static void main(String[] args) {
        // TODO: создайте SavingsAccount и CheckingAccount, положите средства,
        //       вызовите applyMonthly() через List<Account>, выведите балансы
    }
}
