/**
 * Задача 07 — Тема 01 (МИНИ-ПРОЕКТ OPS): Каталог товаров
 *
 * Это ПЕРВЫЙ кусочек сквозного проекта OPS (Order Processing System).
 * Дальше каталог обрастёт заказами, доставкой, оплатой и в финале станет
 * микросервисной системой. Сейчас задача — заложить доменные сущности на
 * чистом ООП: инкапсуляция + полиморфизм + композиция вместе.
 *
 * ЗАДАНИЕ:
 *   1. Product (файл Product.java) — абстрактный товар:
 *      инкапсулированные id, title, priceCents (валидация в конструкторе);
 *      абстрактный метод long shippingCostCents() (стоимость доставки).
 *   2. PhysicalProduct extends Product: поле weightGrams; доставка =
 *      50 коп. за каждые начатые 100 г (например, 250 г → 150 коп.).
 *   3. DigitalProduct extends Product: доставка всегда 0.
 *   4. Catalog (файл Catalog.java) — КОМПОЗИЦИЯ: хранит List<Product>,
 *      методы add(Product), totalGoodsValueCents() (сумма цен),
 *      totalShippingCents() (сумма доставок, полиморфно).
 *   В main соберите каталог из физических и цифровых товаров и выведите итоги.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Товаров в каталоге: 3
 *   Стоимость товаров: 27000 коп.
 *   Стоимость доставки: 300 коп.
 *
 * ТРЕБОВАНИЯ:
 *   - поля Product только private, цена/название валидируются;
 *   - Catalog НЕ наследует List, а хранит его внутри (композиция);
 *   - суммирование доставки идёт через полиморфный shippingCostCents()
 *     без instanceof.
 *
 * ПОДСКАЗКА:
 *   Доставка по весу: ((weightGrams + 99) / 100) * 50.
 *   Делайте по шагам: Product → два подтипа → Catalog → main.
 */

public class Task07 {
    public static void main(String[] args) {
        // TODO: создайте Catalog, добавьте PhysicalProduct и DigitalProduct,
        //       выведите количество, сумму товаров и сумму доставки
    }
}
