class OrderReceiptRenderer {
    public String render(Order order) {
        // TODO: собрать текст чека (заголовок, строки позиций, итог)
        // ВНИМАНИЕ: итог берите у OrderTotalCalculator, не дублируйте формулу
        return "";
    }
}
