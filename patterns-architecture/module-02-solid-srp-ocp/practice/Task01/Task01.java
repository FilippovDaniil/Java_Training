/**
 * Задача 01 — Тема 02: SRP — разбить God-класс
 *
 * ЗАДАНИЕ:
 *   Дан класс-данные Order (файл Order.java) — его НЕ меняем.
 *   Раньше вся логика лежала в одном God-классе: он и считал сумму,
 *   и форматировал чек, и «сохранял» заказ. Разнесите эти ТРИ ответственности
 *   по трём классам (по одной причине для изменения у каждого):
 *     - OrderTotalCalculator: long total(Order) — сумма позиций;
 *     - OrderReceiptRenderer: String render(Order) — текст чека;
 *     - OrderRepository: save(Order) / findById(String) — хранение в памяти (Map).
 *   В main посчитайте сумму, отрендерьте чек, сохраните и найдите заказ.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Чек заказа A-1:
 *     - 8000
 *     - 12000
 *   Итого: 20000
 *   Найден после сохранения: A-1
 *
 * ТРЕБОВАНИЯ:
 *   - каждая ответственность — отдельный класс (расчёт / вывод / хранение);
 *   - Order остаётся «глупой» структурой данных (бизнес-правил в нём нет);
 *   - ни один из трёх классов не делает работу другого.
 *
 * ПОДСКАЗКА:
 *   Проверка SRP: у каждого класса должна быть ровно одна причина меняться.
 *   Поменялась формула суммы → правим только Calculator, и т.д.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: создайте Order, посчитайте total, отрендерьте чек,
        //       сохраните в OrderRepository и найдите по id
    }
}
