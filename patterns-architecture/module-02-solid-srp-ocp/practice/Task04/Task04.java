/**
 * Задача 04 — Тема 02: OCP — семейство политик скидок
 *
 * ЗАДАНИЕ:
 *   Цену заказа корректирует политика скидки. Сделайте так, чтобы добавление
 *   новой политики не требовало правок калькулятора:
 *     - интерфейс DiscountPolicy (файл DiscountPolicy.java):
 *         long apply(long priceCents);   // возвращает цену после скидки
 *     - NoDiscount (без изменений), PercentDiscount(percent) (минус N%);
 *     - PriceCalculator (файл PriceCalculator.java): принимает DiscountPolicy
 *       в конструкторе и метод long finalPrice(long base) применяет политику —
 *       калькулятор ЗАКРЫТ для модификации;
 *     - ДОБАВЬТЕ ThresholdDiscount: если цена >= порога, минус фикс. сумма
 *       (создаётся без правок PriceCalculator).
 *   В main прогоните одну цену через разные политики.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Без скидки: 10000
 *   -10%: 9000
 *   -500 при цене >= 8000: 9500
 *
 * ТРЕБОВАНИЯ:
 *   - PriceCalculator не знает о конкретных политиках, только об интерфейсе;
 *   - новая политика = новый класс, существующие не трогаем;
 *   - скидка не уводит цену ниже 0.
 *
 * ПОДСКАЗКА:
 *   Это связка OCP + DIP (тема 03): калькулятор зависит от абстракции
 *   DiscountPolicy, а конкретику получает снаружи.
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: примените к одной базовой цене NoDiscount, PercentDiscount(10),
        //       ThresholdDiscount(порог, сумма) через PriceCalculator
    }
}
