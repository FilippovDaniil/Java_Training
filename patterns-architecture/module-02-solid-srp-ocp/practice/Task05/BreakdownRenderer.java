// ТОЛЬКО форматирует (SRP). Ничего не считает — получает готовые суммы.
class BreakdownRenderer {
    public String render(long priceCents, long taxCents, long totalCents) {
        // TODO: собрать текст разбивки (Цена / Налог / Итого)
        return "";
    }
}
