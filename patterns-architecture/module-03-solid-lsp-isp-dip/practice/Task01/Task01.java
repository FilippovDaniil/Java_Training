/**
 * Задача 01 — Тема 03: LSP — Квадрат и Прямоугольник
 *
 * ЗАДАНИЕ:
 *   Наивное "class Square extends Rectangle" нарушает LSP: код, меняющий
 *   ширину и высоту прямоугольника независимо, ломается для квадрата.
 *   Постройте иерархию ПРАВИЛЬНО, по поведению:
 *     - интерфейс Shape (файл Shape.java): long area();
 *     - Rectangle (width, height) и Square (side) — НЕЗАВИСИМЫЕ реализации
 *       Shape (Square НЕ наследует Rectangle).
 *   В main сложите фигуры в List<Shape> и посчитайте площади — подстановка
 *   любого Shape работает корректно.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Прямоугольник 5x4: 20
 *   Квадрат 4: 16
 *   Сумма площадей: 36
 *
 * ТРЕБОВАНИЯ:
 *   - Square НЕ extends Rectangle;
 *   - оба взаимозаменяемы как Shape без сюрпризов в поведении;
 *   - никаких сеттеров, ломающих инвариант фигуры.
 *
 * ПОДСКАЗКА:
 *   LSP-вопрос: «можно ли подставить подтип вместо базового, не сломав
 *   ожидания вызывающего кода?». Если setWidth у квадрата меняет и высоту —
 *   нельзя. Решение — не наследовать, а реализовать общий контракт Shape.
 */

import java.util.List;

public class Task01 {
    public static void main(String[] args) {
        // TODO: List<Shape> из Rectangle и Square, посчитайте площади и сумму
    }
}
