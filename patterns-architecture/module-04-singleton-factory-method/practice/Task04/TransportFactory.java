class TransportFactory {
    public static Transport create(String type) {
        // TODO: по "truck" -> new Truck(), "ship" -> new Ship(),
        //       иначе throw new IllegalArgumentException
        return null;
    }
}
