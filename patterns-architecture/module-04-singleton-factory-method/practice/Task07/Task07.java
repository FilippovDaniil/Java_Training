/**
 * Задача 07 — Тема 04 (МИНИ-ПРОЕКТ OPS): нумерация и создание заказов
 *
 * Развитие OPS. Объедините два порождающих паттерна:
 *   - SINGLETON — единый генератор номеров заказов на всю систему;
 *   - FACTORY — фабрика собирает готовый Order, сама проставляя номер.
 *
 * ЗАДАНИЕ:
 *   1. OrderNumberGenerator (файл OrderNumberGenerator.java) — Singleton
 *      (любой надёжный способ: eager/holder/enum), метод String next()
 *      выдаёт "OPS-1", "OPS-2", ... (общий счётчик).
 *   2. Order (файл Order.java) — данные: number, sku, priceCents (готов).
 *   3. OrderFactory (файл OrderFactory.java): метод
 *      Order create(String sku, long priceCents) — берёт номер у
 *      OrderNumberGenerator (Singleton) и возвращает собранный Order.
 *   В main создайте 2–3 заказа через фабрику и выведите их номера —
 *   нумерация сквозная (доказывает, что генератор один на всех).
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Заказ OPS-1: sku=BOOK-1, 50000
 *   Заказ OPS-2: sku=PEN-7, 12000
 *   Заказ OPS-3: sku=MUG-3, 8000
 *
 * ТРЕБОВАНИЯ:
 *   - номер заказа выдаёт ТОЛЬКО Singleton-генератор (нигде не дублируется счётчик);
 *   - клиент (main) не присваивает номера руками — это делает фабрика;
 *   - две фабрики (если создать две) всё равно используют один генератор →
 *     номера не повторяются.
 *
 * ПОДСКАЗКА:
 *   Это «репетиция» будущего OrderService. Singleton здесь оправдан как единый
 *   источник сквозной нумерации; в теме 16 его роль возьмёт на себя DI-контейнер.
 */

public class Task07 {
    public static void main(String[] args) {
        // TODO: создайте OrderFactory, создайте через неё несколько заказов,
        //       выведите их номера/sku/цены
    }
}
