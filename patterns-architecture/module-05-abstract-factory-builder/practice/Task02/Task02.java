/**
 * Задача 02 — Тема 05: Builder с валидацией в build()
 *
 * ЗАДАНИЕ:
 *   Реализуйте User (файл User.java) через Builder, где build() ПРОВЕРЯЕТ
 *   обязательные поля:
 *     - обязательные: name, email (непустые);
 *     - опциональные: age (по умолчанию 0), city (по умолчанию "");
 *     - build() при пустом name или email бросает IllegalStateException.
 *   В main соберите корректного пользователя, затем попробуйте собрать без
 *   email и поймайте исключение.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   User{name=Аня, email=anya@mail.com, age=25, city=Москва}
 *   Ошибка сборки: email обязателен
 *
 * ТРЕБОВАНИЯ:
 *   - проверка обязательных полей живёт ИМЕННО в build(), а не в сеттерах;
 *   - опциональные поля имеют значения по умолчанию;
 *   - объект User неизменяем после построения.
 *
 * ПОДСКАЗКА:
 *   Валидация в build() — ключевая выгода Builder: объект либо собирается
 *   корректным, либо не собирается вовсе (нельзя получить «полу-User»).
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: соберите корректного User; затем без email — поймайте IllegalStateException
    }
}
