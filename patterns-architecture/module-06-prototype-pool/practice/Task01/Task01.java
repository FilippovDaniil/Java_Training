/**
 * Задача 01 — Тема 06: Prototype через copy-конструктор
 *
 * ЗАДАНИЕ:
 *   Реализуйте Document (файл Document.java) с copy-конструктором:
 *     - поля: title (String), tags (List<String>);
 *     - обычный конструктор Document(String title);
 *     - copy-конструктор Document(Document other), который копирует title и
 *       создаёт НОВЫЙ список тегов (не делит ссылку с оригиналом);
 *     - метод addTag(String) и геттеры.
 *   В main создайте оригинал с парой тегов, сделайте копию, добавьте тег ТОЛЬКО
 *   в копию и покажите, что оригинал не изменился.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Оригинал теги: [a, b]
 *   Копия теги: [a, b, c]
 *   Оригинал после правки копии: [a, b]
 *
 * ТРЕБОВАНИЯ:
 *   - copy-конструктор делает ГЛУБОКУЮ копию списка тегов (new ArrayList<>(...));
 *   - правка копии не затрагивает оригинал;
 *   - без Cloneable — только copy-конструктор.
 *
 * ПОДСКАЗКА:
 *   Ключевая строка: this.tags = new ArrayList<>(other.tags);
 *   Если написать this.tags = other.tags — получите общий список (баг).
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: создайте Document, скопируйте, измените копию, сравните с оригиналом
    }
}
