/**
 * Задача 07 — Тема 06 (МИНИ-ПРОЕКТ OPS): шаблоны заказов и пул соединений
 *
 * Развитие OPS. Объедините два порождающих паттерна:
 *   - PROTOTYPE (+ реестр) — типовые шаблоны заказов, из которых «штампуют»
 *     конкретные заказы независимыми копиями;
 *   - OBJECT POOL — переиспользование «тяжёлых» соединений с платёжным шлюзом
 *     при оформлении заказов.
 *
 * ЗАДАНИЕ:
 *   1. OrderTemplate (файл OrderTemplate.java): name (String) и items
 *      (List<String>); метод copy() — ГЛУБОКАЯ копия (новый список); addItem(String).
 *   2. TemplateRegistry (файл TemplateRegistry.java): register(String key,
 *      OrderTemplate proto) и create(String key) → proto.copy().
 *   3. GatewayConnection (файл GatewayConnection.java): поле id, конструктор
 *      печатает "открыт шлюз #id".
 *   4. GatewayPool (файл GatewayPool.java): acquire()/release() с переиспользованием.
 *   В main: зарегистрируйте шаблон "standard" (пара позиций), создайте из него
 *   ДВА заказа, измените каждый независимо; затем acquire соединение из пула,
 *   «оформите» заказ, release, снова acquire — соединение переиспользуется.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Заказ A items: [доставка, страховка, подарок]
 *   Заказ B items: [доставка, страховка, скидка]
 *   открыт шлюз #1
 *   повторный acquire вернул #1 (переиспользование)
 *
 * ТРЕБОВАНИЯ:
 *   - create(key) возвращает независимую копию шаблона (правка одного заказа не
 *     влияет на другой и на шаблон);
 *   - пул не создаёт новое соединение, если есть свободное;
 *   - оба паттерна работают в одном сценарии оформления заказа.
 *
 * ПОДСКАЗКА:
 *   Prototype экономит на повторной настройке типовых заказов, Pool — на
 *   повторном открытии дорогих соединений. В реальном OPS пулом управлял бы
 *   готовый драйвер (например, HikariCP), а шаблоны хранились бы в каталоге.
 */

public class Task07 {
    public static void main(String[] args) {
        // TODO: 1) TemplateRegistry: register("standard", ...), create два заказа,
        //          изменить каждый независимо, вывести их items;
        //       2) GatewayPool: acquire, release, повторный acquire (переиспользование)
    }
}
