class CelsiusAdapter implements TemperatureSensor {
    // TODO: поле FahrenheitSensor sensor + конструктор CelsiusAdapter(FahrenheitSensor sensor)

    @Override
    public double readCelsius() {
        // TODO: (sensor.readFahrenheit() - 32) * 5.0 / 9.0
        return 0;
    }
}
