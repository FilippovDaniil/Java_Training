/**
 * Задача 01 — Тема 09: Composite (дерево файлов)
 *
 * ЗАДАНИЕ:
 *   Реализуйте файловое дерево с единым подсчётом размера:
 *     - интерфейс FileSystemNode (файл FileSystemNode.java): long size();
 *     - FileLeaf (файл FileLeaf.java): хранит размер, size() возвращает его;
 *     - Directory (файл Directory.java): хранит List<FileSystemNode>, метод
 *       add(FileSystemNode), size() рекурсивно суммирует размеры детей.
 *   В main соберите дерево: каталог с файлами и вложенным каталогом, посчитайте
 *   общий размер ОДНИМ вызовом size() на корне.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Размер дерева: 40
 *
 * ТРЕБОВАНИЯ:
 *   - size() вызывается единообразно и на файле, и на каталоге (без instanceof);
 *   - Directory рекурсивно обходит детей;
 *   - в дерево можно вкладывать каталоги в каталоги.
 *
 * ПОДСКАЗКА:
 *   У листа size() тривиален, у композита — сумма size() всех детей. Клиент
 *   не различает лист и контейнер.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: соберите дерево из FileLeaf и Directory, выведите size() корня
    }
}
