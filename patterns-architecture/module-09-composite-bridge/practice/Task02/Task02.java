/**
 * Задача 02 — Тема 09: Composite (орг-структура, headcount)
 *
 * ЗАДАНИЕ:
 *   Постройте орг-структуру и посчитайте численность единообразно:
 *     - интерфейс OrgUnit (файл OrgUnit.java): int headcount();
 *     - Employee (файл Employee.java) — лист: headcount() = 1;
 *     - Department (файл Department.java) — композит: содержит List<OrgUnit>,
 *       add(OrgUnit), headcount() рекурсивно суммирует.
 *   В main соберите компанию: отдел с сотрудниками и вложенным под-отделом,
 *   выведите общую численность.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Сотрудников в компании: 5
 *
 * ТРЕБОВАНИЯ:
 *   - headcount() вызывается одинаково на сотруднике и на отделе;
 *   - отделы вкладываются друг в друга;
 *   - без instanceof — поведение полиморфное.
 *
 * ПОДСКАЗКА:
 *   Та же идея, что в Task01 (файлы), но операция — подсчёт людей. Лист = 1,
 *   композит = сумма детей.
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: соберите Department с Employee и вложенным Department, выведите headcount()
    }
}
