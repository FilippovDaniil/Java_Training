/**
 * Задача 02 — Тема 10: Strategy через лямбду (функциональный интерфейс)
 *
 * ЗАДАНИЕ:
 *   Если у стратегии один метод, её можно задать лямбдой без отдельного класса.
 *   В ЭТОМ ОДНОМ ФАЙЛЕ:
 *     - объявите функциональный интерфейс PriceRule с методом long apply(long base)
 *       (можно вложенным в Task02);
 *     - в main создайте несколько стратегий ЛЯМБДАМИ: без изменений, +20% налог,
 *       минус 500 фикс.;
 *     - примените каждую к одной базовой цене и выведите результат.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   base: 10000
 *   +20%: 12000
 *   -500: 9500
 *
 * ТРЕБОВАНИЯ:
 *   - PriceRule — функциональный интерфейс (ровно один абстрактный метод);
 *   - стратегии заданы лямбдами, без объявления классов-реализаций;
 *   - одна и та же переменная типа PriceRule принимает разные лямбды.
 *
 * ПОДСКАЗКА:
 *   PriceRule tax = base -> base + base * 20 / 100;
 *   Лямбда — это лёгкая реализация Strategy для интерфейса с одним методом.
 */

public class Task02 {

    // TODO: объявите функциональный интерфейс PriceRule { long apply(long base); }

    public static void main(String[] args) {
        // TODO: создайте лямбда-стратегии (без изменений, +20%, -500) и примените их
    }
}
