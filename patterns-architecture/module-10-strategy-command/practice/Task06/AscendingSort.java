import java.util.Arrays;

class AscendingSort implements SortStrategy {
    @Override
    public void sort(int[] arr) {
        // TODO: отсортировать по возрастанию (например, Arrays.sort(arr))
    }
}
