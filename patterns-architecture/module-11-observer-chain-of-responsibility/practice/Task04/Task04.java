/**
 * Задача 04 — Тема 11: Chain of Responsibility (согласование расходов)
 *
 * ЗАДАНИЕ:
 *   Согласование суммы расхода уходит к тому, у кого хватает полномочий:
 *     - абстрактный Approver (файл Approver.java): next, setNext(Approver),
 *       abstract String approve(int amount), passToNext(int);
 *     - TeamLead (до 1000), Director (до 100000), CEO (любая сумма);
 *       каждый: если сумма в его лимите — "<Должность> одобрил <amount>",
 *       иначе — дальше по цепочке.
 *   В main соберите цепочку TeamLead → Director → CEO и согласуйте суммы
 *   500, 50000 и 5000000.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   TeamLead одобрил 500
 *   Director одобрил 50000
 *   CEO одобрил 5000000
 *
 * ТРЕБОВАНИЯ:
 *   - каждый уровень обрабатывает только «свой» диапазон сумм;
 *   - превышение лимита → передача следующему по цепочке;
 *   - добавление нового уровня согласования = новый Approver, без правок других.
 *
 * ПОДСКАЗКА:
 *   Это типичный пример CoR. teamLead.setNext(director).setNext(ceo).
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: соберите цепочку согласующих, вызовите approve(500/50000/5000000)
    }
}
