/**
 * Задача 01 — Тема 14: Information Expert
 *
 * ЗАДАНИЕ:
 *   Поведение — туда, где данные. Пусть заказ сам считает свою сумму:
 *     - OrderLine (файл OrderLine.java): sku, qty (int), priceCents (long);
 *       метод subtotal() = qty * priceCents (строка — эксперт по своей сумме);
 *     - Order (файл Order.java): хранит List<OrderLine>, addLine(...),
 *       total() суммирует subtotal() строк (заказ — эксперт по своей сумме).
 *   В main соберите заказ из нескольких строк и выведите итог — НЕ доставая
 *   цены наружу руками.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Итого заказа: 32000   (2*10000 + 3*4000)
 *
 * ТРЕБОВАНИЯ:
 *   - сумму строки считает OrderLine, сумму заказа — Order (эксперты по данным);
 *   - main не перебирает чужие поля для расчёта (никаких getPrice()*getQty() снаружи);
 *   - поведение размещено там, где лежат нужные данные.
 *
 * ПОДСКАЗКА:
 *   Information Expert борется с «анемичной моделью»: данные и работа над ними —
 *   в одном классе.
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: соберите Order из OrderLine, выведите total()
    }
}
