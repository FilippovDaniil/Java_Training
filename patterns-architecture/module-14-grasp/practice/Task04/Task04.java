/**
 * Задача 04 — Тема 14: High Cohesion (высокая связность)
 *
 * ЗАДАНИЕ:
 *   Дан MixedHelper (файл MixedHelper.java) — низкосвязный класс: он и проверяет
 *   пароль, и форматирует профиль (две несвязанные обязанности). Разнесите их по
 *   двум связным классам (НЕ используйте MixedHelper в решении — он оставлен как «до»):
 *     - PasswordChecker (файл PasswordChecker.java): boolean isStrong(String pwd)
 *       (длина >= 8);
 *     - ProfileFormatter (файл ProfileFormatter.java): String format(String name,
 *       int age) → "name (age)".
 *   В main проверьте пароль и отформатируйте профиль, используя оба класса.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   Пароль надёжный: true
 *   Профиль: Аня (25)
 *
 * ТРЕБОВАНИЯ:
 *   - каждая обязанность — в своём связном классе (проверка пароля / форматирование);
 *   - классы не делают «чужую» работу;
 *   - High Cohesion + SRP: одна связная зона ответственности на класс.
 *
 * ПОДСКАЗКА:
 *   Низкая связность («класс обо всём») — частый источник God Object (тема 22).
 *   Признак: в описании класса есть несвязанные «И».
 */

public class Task04 {
    public static void main(String[] args) {
        // TODO: проверьте пароль через PasswordChecker, отформатируйте профиль через ProfileFormatter
    }
}
