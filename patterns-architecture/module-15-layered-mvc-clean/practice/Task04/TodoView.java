import java.util.List;

// View: рисует переданный список (без логики).
class TodoView {
    public void render(List<String> tasks) {
        // TODO: для каждой задачи напечатать "(i+1). задача"
    }
}
