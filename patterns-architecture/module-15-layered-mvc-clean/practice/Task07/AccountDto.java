// DTO для Presentation (наружу отдаётся он, а не доменный Account).
class AccountDto {
    // TODO: поля id, balanceCents + конструктор

    public String getId() { return ""; }          // TODO
    public long getBalanceCents() { return 0; }    // TODO
}
