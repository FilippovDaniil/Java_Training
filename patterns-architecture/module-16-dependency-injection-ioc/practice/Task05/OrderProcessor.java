// Достаёт зависимость САМ из локатора (зависимость скрыта — её нет в конструкторе).
class OrderProcessor {
    public void process() {
        // TODO: Notifier n = ServiceLocator.get(Notifier.class);
        //       n.notify("заказ обработан");
    }
}
