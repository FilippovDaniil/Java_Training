class OrderService {
    // TODO: поле final OrderRepository repo + конструктор OrderService(OrderRepository repo)

    public void place(String id, long amountCents) {
        // TODO: repo.save(new Order(id, amountCents))
    }

    public long amount(String id) {
        // TODO: repo.findById(id).getAmountCents()
        return 0;
    }
}
