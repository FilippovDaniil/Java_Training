// Ядро: зависит от порта AccountStore, тестируется с любым его адаптером.
class DepositService {
    // TODO: поле final AccountStore store + конструктор DepositService(AccountStore store)

    public void deposit(String id, long amount) {
        // TODO: load(id) → deposit(amount) → save(account)
    }
}
