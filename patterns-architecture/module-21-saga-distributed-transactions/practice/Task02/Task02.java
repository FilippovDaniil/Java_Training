/**
 * Задача 02 — Тема 21: компенсирующее действие
 *
 * ЗАДАНИЕ:
 *   Добавьте шагу обратное действие (компенсацию):
 *     - интерфейс SagaStep (файл SagaStep.java): void execute(); void compensate();
 *     - ReserveStep (файл ReserveStep.java): execute() печатает "зарезервировано",
 *       compensate() печатает "резерв снят".
 *   В main выполните шаг, затем компенсируйте его — увидите прямое и обратное действие.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   зарезервировано
 *   резерв снят
 *
 * ТРЕБОВАНИЯ:
 *   - compensate() семантически ОТМЕНЯЕТ эффект execute() (новое действие, не rollback БД);
 *   - оба метода — у одного шага;
 *   - компенсация осмысленна (снять резерв, а не «как будто не было»).
 *
 * ПОДСКАЗКА:
 *   Компенсация — это обратное бизнес-действие: списали → вернуть, зарезервировали
 *   → снять резерв. Похоже на undo у Command (тема 10).
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: создайте ReserveStep, вызовите execute(), затем compensate()
    }
}
