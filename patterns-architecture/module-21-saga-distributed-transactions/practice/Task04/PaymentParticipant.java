class PaymentParticipant {
    public void register(EventBus bus) {
        // TODO: bus.subscribe(OrderCreated.class, e -> {
        //         напечатать "оплата проведена";
        //         bus.publish(new PaymentDone(e.orderId()));
        //       });
    }
}
