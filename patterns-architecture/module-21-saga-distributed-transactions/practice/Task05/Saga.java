import java.util.ArrayList;
import java.util.List;

class Saga {
    public void run(List<SagaStep> steps) {
        // TODO: List<SagaStep> done = new ArrayList<>();
        //       for (s : steps) {
        //           if (!s.execute()) { for (i = done.size()-1; i>=0; i--) done.get(i).compensate(); return; }
        //           done.add(s);
        //       }
    }
}
