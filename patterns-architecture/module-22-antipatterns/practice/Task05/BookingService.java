class BookingService {
    public String book(BookingRequest req) {
        // TODO: "Бронь: " + req.guest() + ", " + req.room() + ", " + req.nights()
        //        + " ноч., завтрак=" + req.breakfast()
        return "";
    }
}
