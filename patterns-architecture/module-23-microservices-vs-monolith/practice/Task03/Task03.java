/**
 * Задача 03 — Тема 23: контракт сервиса — локальная vs «удалённая» реализация
 *
 * ЗАДАНИЕ:
 *   Граница между сервисами — это контракт. За ним может быть локальная
 *   реализация (модульный монолит) или сетевой вызов (микросервис) — клиент один.
 *     - PricingService (файл PricingService.java) — контракт: long priceOf(String sku);
 *     - LocalPricingService (файл ...): in-process, возвращает sku.length() * 1000;
 *     - RemotePricingClient (файл ...): имитирует сетевой вызов — печатает
 *       "[remote] вызов priceOf(sku)" и возвращает тот же результат.
 *   В main вызовите priceOf через обе реализации (тип — PricingService).
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   local: 4000
 *   [remote] вызов priceOf(BOOK)
 *   remote: 4000
 *
 * ТРЕБОВАНИЯ:
 *   - клиент работает с типом PricingService, не зная, локальный это вызов или сетевой;
 *   - обе реализации взаимозаменяемы (один контракт);
 *   - переход монолит → микросервис = смена реализации контракта, не клиента.
 *
 * ПОДСКАЗКА:
 *   Это та же идея, что DIP/порт (темы 03, 17): зависимость на контракт. RemoteClient
 *   в реальности слал бы REST/gRPC; здесь — печать, имитирующая сетевой вызов.
 */

public class Task03 {
    public static void main(String[] args) {
        // TODO: вызовите priceOf("BOOK") через LocalPricingService и RemotePricingClient
    }
}
