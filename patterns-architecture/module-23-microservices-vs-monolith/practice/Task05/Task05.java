/**
 * Задача 05 — Тема 23: декомпозиция по бизнес-возможностям
 *
 * ЗАДАНИЕ:
 *   Два модуля по возможностям (Каталог и Заказы); КАЖДЫЙ владеет своими данными,
 *   а ссылается на чужие — по идентификатору, через публичный API:
 *     - CatalogModule (файл ...): String titleOf(String sku); CatalogModuleImpl
 *       владеет своим хранилищем (sku → название) + seed(sku, title);
 *     - OrderModule (файл OrderModule.java): владеет СВОИМ счётчиком заказов;
 *       зависит от CatalogModule; place(String sku) → увеличивает счётчик и
 *       возвращает "заказан: " + catalog.titleOf(sku); ordersCount().
 *   В main: наполните каталог, оформите заказ по sku, выведите результат и число заказов.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   заказан: Чистый код
 *   Заказов: 1
 *
 * ТРЕБОВАНИЯ:
 *   - каждый модуль владеет своими данными (Каталог — товарами, Заказы — заказами);
 *   - OrderModule ссылается на товар по sku и берёт название через API каталога
 *     (не лезет в его хранилище);
 *   - границы проведены по бизнес-возможностям, а не по слоям.
 *
 * ПОДСКАЗКА:
 *   Это Bounded Context (тема 18) на уровне модулей: своя модель и свои данные у
 *   каждого, связь — по id через контракт. Хорошая граница для будущего сервиса.
 */

public class Task05 {
    public static void main(String[] args) {
        // TODO: CatalogModuleImpl + seed; OrderModule(catalog); place(sku); вывести ordersCount()
    }
}
