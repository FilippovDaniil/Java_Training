/**
 * Задача 01 — Тема 24: Message Queue (точка-точка)
 *
 * ЗАДАНИЕ:
 *   Реализуйте очередь сообщений FIFO (продюсер кладёт, один консьюмер забирает):
 *     - MessageQueue (файл MessageQueue.java): send(String msg); String receive()
 *       (FIFO; null/пусто, если очередь пуста); boolean isEmpty().
 *   В main отправьте 3 сообщения и заберите их по очереди — в том же порядке.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   получено: m1
 *   получено: m2
 *   получено: m3
 *
 * ТРЕБОВАНИЯ:
 *   - порядок FIFO (первым пришёл — первым ушёл);
 *   - каждое сообщение получает ОДИН consumer (точка-точка);
 *   - отправка и получение развязаны (можно отправить все, потом забрать).
 *
 * ПОДСКАЗКА:
 *   Внутри — ArrayDeque<String>: send → offer, receive → poll. Так продюсер не
 *   ждёт консьюмера (асинхронность во времени).
 */

public class Task01 {
    public static void main(String[] args) {
        // TODO: отправьте 3 сообщения; пока !isEmpty() — receive и печатайте
    }
}
