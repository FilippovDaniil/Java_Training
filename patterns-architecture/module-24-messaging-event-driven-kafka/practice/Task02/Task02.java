/**
 * Задача 02 — Тема 24: Pub-Sub (топик, много подписчиков)
 *
 * ЗАДАНИЕ:
 *   Реализуйте топик, рассылающий КАЖДОЕ событие ВСЕМ подписчикам:
 *     - Topic (файл Topic.java): subscribe(Consumer<String> sub); publish(String event)
 *       уведомляет всех подписчиков.
 *   В main подпишите двух подписчиков (лямбдами, с разными префиксами) и
 *   опубликуйте событие — получат оба.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   A получил: заказ создан
 *   B получил: заказ создан
 *
 * ТРЕБОВАНИЯ:
 *   - событие получают ВСЕ подписчики (в отличие от очереди — там один);
 *   - продюсер не знает, кто и сколько слушает;
 *   - новый подписчик добавляется без правок издателя.
 *
 * ПОДСКАЗКА:
 *   Topic хранит List<Consumer<String>>; publish перебирает и вызывает accept(event).
 *   Это Observer (тема 11) в духе обмена сообщениями.
 */

public class Task02 {
    public static void main(String[] args) {
        // TODO: Topic; subscribe два лямбда-подписчика; publish("заказ создан")
    }
}
