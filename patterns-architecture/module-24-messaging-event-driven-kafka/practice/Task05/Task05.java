/**
 * Задача 05 — Тема 24: Dead Letter Queue (DLQ)
 *
 * ЗАДАНИЕ:
 *   «Ядовитое» сообщение нельзя обрабатывать вечно — после неудачи его шлют в DLQ:
 *     - DeadLetterProcessor (файл DeadLetterProcessor.java): хранит список DLQ;
 *       process(String msg): если msg начинается с "BAD" → считать обработку
 *       неуспешной, положить в DLQ и напечатать "в DLQ: msg"; иначе напечатать
 *       "обработано: msg"; dlqSize().
 *   В main обработайте "ok-1", "BAD-2", "ok-3" и выведите размер DLQ.
 *
 * ОЖИДАЕМЫЙ ВЫВОД (пример):
 *   обработано: ok-1
 *   в DLQ: BAD-2
 *   обработано: ok-3
 *   В DLQ: 1
 *
 * ТРЕБОВАНИЯ:
 *   - сообщение, которое не удалось обработать, уходит в DLQ (не теряется и не
 *     блокирует остальные);
 *   - «хорошие» сообщения обрабатываются нормально;
 *   - DLQ хранит проблемные сообщения для последующего разбора.
 *
 * ПОДСКАЗКА:
 *   В реальной системе в DLQ попадают после N повторов (см. Task06). Здесь — после
 *   первой неудачи, для наглядности.
 */

public class Task05 {
    public static void main(String[] args) {
        // TODO: process("ok-1"), process("BAD-2"), process("ok-3"); вывести dlqSize()
    }
}
