// Раньше вызывал случайность напрямую; теперь берёт число из шва (тестируемо).
class Dice {
    // TODO: поле final NumberSource source + конструктор Dice(NumberSource source)

    public int roll() {
        // TODO: source.next()
        return 0;
    }
}
