# Задача 06 — Модуль 49: LLM-специфика в дизайне

## ЗАДАНИЕ
Перечислите особенности LLM, влияющие на дизайн системы, и решения для каждой.

## ВХОДНЫЕ ДАННЫЕ
Система использует LLM: генерация медленная, оплата за токены, ограничен контекст, модель — внешняя зависимость, вывод недетерминирован.

## ТРЕБОВАНИЯ
- Медленная генерация → token streaming (отдавать по токенам).
- Стоимость per-token → cost растёт с длиной prompt+ответа (новая ось — модуль 7).
- Лимит context window → RAG/чанкинг/суммаризация (нельзя влить всё).
- Prompt caching для повторяющегося префикса.
- Недетерминизм/галлюцинации → guardrails/валидация вывода/eval.
- Внешняя зависимость → fallback/timeout/circuit breaker (модуль 23).

## ФОРМАТ РЕЗУЛЬТАТА
Таблица: особенность LLM → следствие/решение для дизайна (+ модуль).

## КРИТЕРИИ ПРИЁМКИ
- [ ] Медленная генерация → token streaming.
- [ ] Стоимость per-token → GPU/token cost как новая ось (модуль 7).
- [ ] Context window лимит → RAG/чанкинг/суммаризация; prompt caching для префикса.
- [ ] Недетерминизм → guardrails/валидация/eval; внешняя модель → fallback/timeout/circuit breaker (модуль 23).

## ПОДСКАЗКА
Генерация медленная → streaming. Per-token → cost-ось (7). Context window → RAG/чанкинг. Prompt caching. Галлюцинации → guardrails/eval. Внешняя модель → fallback/circuit breaker (23).

---
<!-- Ваше решение ниже -->
